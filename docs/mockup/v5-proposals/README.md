# 案件详情页 v5 概念方案（提案，未批准）

> 状态：**设计探索稿**。2026-09-19 应叶森要求，在功能不动的前提下对案件详情页做三个方向的重设计效果图。
> 尚未选定方向、未写实施方案、未改任何产品代码——不是 v4 验收基准的替代品。

## 文件

| 文件 | 方向 | 一句话 |
|---|---|---|
| `index.html` | 导航页 | 三案对比入口，附缩略示意 |
| `a-ledger.html` | A 宣纸墨线 | 纸感排印：暖纸底、细墨线分节、汉字序号环节、点线引导金额；结构靠排印不靠卡片投影 |
| `b-caseline.html` | B 进程主线 | 深墨头版内嵌一条发光横向办案主线（完成/当前/风险/今天游标），工作区以倒计时表盘领衔 |
| `c-cockpit.html` | C 精工仪表 | 冷调精密：头部 KPI 数据条 + 分段甘特进程标尺（带「今天」游标），等宽数字、1px 边线 |

三个方案使用同一案件样例数据（王建国与宏远建设集团有限公司建设工程施工合同纠纷，一审、举证质证环节），
功能块与现版完全对齐：案卷头、程序切换、四页签（案件档案/办案进程/委托与财务/审批用印）、环节范围、
待办（逾期/一周内/之后/等待他人）、材料（来源/证据要点）、经办记录（沟通/寄收件/研判）、侧栏（团队/金额/等待审批）。

## 设计前提（继承自既定决策，不因换皮改变）

- 功能、数据、权限、Server Action 均不变；仅版式与视觉。
- 案件详情页不使用宋体；数字/日期用等宽字体。
- 一屏一个主操作（顶栏「添加记录」）；敏感信息页内明文。
- 当事人上下排列、诉/辩圆标分隔（A 方案对阵行、B 方案头版对阵行均沿用该语言）。

## 验收方式

浏览器直接打开各 HTML（无外部依赖、无字体加载）。均为桌面版示意（内容宽 1280–1320px）。

## 后续（选定方向后）

1. 按「改文档优先于改代码」：将选定方向写成 `docs/UI-MATTER-DETAIL-v5-PLAN.md` 并更新 `AGENTS.md` 相应条目。
2. 视觉落在 `src/components/patterns/`（moan.tsx / moan-pages.css 对应区块），不动数据层。
