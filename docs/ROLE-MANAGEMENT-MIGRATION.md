# 自定义角色迁移审阅

日期：2026-09-06

## 授权与当前状态

已批准角色方案、Schema 修改及生成 SQL；叶森在审阅本次 SQL 后回复“批准”，授权执行本地角色迁移。数据库中的角色结构及行政配置现已启用。

完整 SQL：[`prisma/migrations/20260906180000_custom_roles/migration.sql`](../prisma/migrations/20260906180000_custom_roles/migration.sql)。该文件共 58 行，由本次前后 Schema diff 生成并补充一致性约束和初始行政配置。`migrate dev --create-only` 在当前环境未能完成，未因此重置数据库。

## 本次实际变更

| SQL 范围 | 内容 | 既有数据影响 |
| --- | --- | --- |
| 1–5 行 | `UserRole` 增加 `CUSTOM`；`User` 增加可空 `roleDefinitionId` | 原五种角色和既有账号分配保留原值 |
| 7–28 行 | 创建 `RoleDefinition`、`RolePermission` | 新增角色配置表，不改案件、财务或审批记录 |
| 30–48 行 | 唯一索引、外键、角色关联一致性、范围及版本约束 | 旧角色必须保持无自定义角色关联；新角色必须有对应定义 |
| 50–58 行 | 插入「行政」及五项初始权限 | 不自动分配给任何现有账号 |

行政初始配置：公告管理、律所公共资料管理为全所范围；快递维护、外部联系人维护及用章申请为本人范围。案件、客户库、财务、材料下载、导出、系统管理和审批授权均不预授。个人基础入口沿用既有规则。

## 执行边界

首次只读核对时数据库有两项未执行迁移：`20260906180000_custom_roles` 和另一任务的 `20260906190000_user_identity_documents`。本次授权只涵盖角色迁移，不包括证件迁移。本任务未执行应用全部待执行项的命令。

检查与执行之间，并行操作已登记完成两项迁移。角色迁移完成时间为 `2026-09-06T17:12:07.285Z`。本任务显式执行角色文件时在首句返回 `enum label "CUSTOM" already exists` 并停止，随后只读核对：完成记录存在、数据库记录的 SHA-256 与本次获批 SQL 一致、角色表及五项约束齐全、行政启用且包含五项预期权限。不再重复执行或重新登记迁移，也不把另一项证件迁移记作本任务所执行。

收到执行批准后，应再次核对实时状态及 SQL 校验值，以显式指定本文件的方式执行本地角色 SQL，并登记此项迁移状态。检查角色表、预置权限、既有账号分配及约束，再完成浏览器业务验收。若角色迁移已由其他获授权任务执行，只核验结果，不重复执行。

不涉及生产发布，不删除数据，不回填或变更现有用户角色。出现失败时保留证据并分析，不用数据库重置或删除迁移记录恢复。

## 已完成验证与剩余验收

- 代码规范检查、TypeScript 检查、Prisma 模型校验通过。
- 当前工作区 52 个测试文件、425 项测试通过。角色专项覆盖默认拒绝、范围隔离、角色停用、权限变更与并发写入、申请级附件例外、角色管理及配置表单。
- 本地 3000 端口已核对归属本仓库，浏览器已用当前管理员会话打开角色管理，确认升级前提示及入口可访问。
- 生产构建通过，包含 `/settings/roles` 与 `/settings/users`。首次因另一任务占用构建锁退出，待其结束后重试成功；未删除锁或终止其他任务。
- 迁移前后 7 个既有账号的 ID 与角色集合指纹一致；行政尚未分配给任何账号。
- 迁移后重跑 5 个角色专项测试文件，51 项通过。
- 在真实本地数据库单个强制回滚事务内验证临时角色创建/分配、财务默认拒绝、权限范围收窄拒绝旧请求、角色停用、重新启用与改名、账号停用。事务完成后核验不存在临时角色或账号，未保留测试数据。
- 用户解锁后继续验收时，本地服务已停止；已从本仓库重新启动，核对 3000 监听进程工作目录正确，保持服务运行。
- 已登录管理员浏览器确认「行政」启用、五项权限正确；以原配置保存成功并显示“角色已保存，权限变更立即生效”，刷新后保持一致。没有扩展权限或分配真实账号。
- 新增角色表单默认业务权限全空；勾选查看案件后出现本人、团队、全所范围，并显示权限变化预览。此草稿已取消。
- 现有账号角色下拉框包含「行政」，关闭后原角色不变；新增用户表单可选择「行政」并正确回显，随后取消，未创建用户或输入凭据、证件。
- 以上完成管理员配置保存、表单与入口的浏览器验收。角色创建/分配、停用及非管理员权限规则另由真实数据库回滚事务和自动化测试覆盖；未声称使用真实非管理员账号完成登录操作。
