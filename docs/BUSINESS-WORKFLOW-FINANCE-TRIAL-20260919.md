# 当前状态：全批开发、独立验收及本机切换完成

最终结果见 [业务流程最终验收](BUSINESS-WORKFLOW-FINAL-ACCEPTANCE-20260919.md)。0/A/B/C/D/E 与审批终止已实现；最终独立库通过 43 项结构写入约束、165 项业务断言，本机切换与新样本生成演练通过。主业务库已获最终批准并完成清理、迁移与新样本生成，保留账号配置；详见最终报告执行结果。以下为先前阶段记录，其中“尚未完成”仅描述当时状态。

---

# 财务 B：退款、更正与分成结算验收（全新数据）

退款、应收折让、收款误录冲销、更正确认/退回/撤销、分成计提调整、实际支付及扣回已在独立库完成服务和页面验收。**这不等于批次 B 全部完成：合同追加/调减/总价替代与程序范围、支出原记录的冲销仍待接入；运行 Schema 与主财务库未切换。**

## 当前基线与验证结果

- 独立库：`lawlink_workflow_trial_20260919_96b87b72`；从现行空模型初始化，执行新版 SQL，保留全部合成样本供复核。
- 候选更正表增加结构化申请内容及实际发生日期，保存明确指定的核销撤回、票款撤回和债权处理；未确认申请不产生账务影响。
- 43 项结构约束验证通过（8 项允许、35 项拒绝），5 张新表及 10 条 CHECK 生效；数据库结构与候选模型比较无差异。
- A 回归通过：28 项登记/确认用例、18 项分配用例。
- B 通过 36 项真实数据库用例：超额/跨案/无权拒绝、债权保留或免除、折让释放已核销款、收款剩余全额冲销、原始金额不改写、原分成快照、实际支付/扣回、管理身份无更正确认权、到账确认与更正确认/分成结算分离、自定义权限确认、申请后变化阻止确认、重复和并发只能成功一次、两类审计失败整体回滚、归档拒绝、跨月退款不改写历史收款月份。
- 全仓 83 个测试文件、651 个用例通过；类型检查、ESLint、现行及候选模型校验、生产构建通过。

## 实际页面验收

案件 `UI-7abad617`，只含独立合成数据：

1. 原应收 10 万元、已收并核销 4 万元，发票 10 万元已关联 4 万元，计提分成 1.2 万元。
2. 页面登记实际支付分成 1.2 万元，待支付归零，支付凭据留存。
3. 提交退款 1 万元，指定撤回核销 1 万元、票款关联 1 万元，选择保留债权；待确认阶段净收款仍为 4 万元、应收余额仍为 6 万元。
4. 在确认弹窗复核原收款、原因、凭据、债权处理、两项关联撤回后确认：净收款 3 万元、应收余额 7 万元、发票未关联 7 万元。
5. 分成原计提保留 1.2 万元，更正 -3000 元，有效计提 9000 元；实际已支付仍为 1.2 万元，显示待扣回 3000 元。
6. 页面登记实际扣回 3000 元及凭据：已扣回 3000 元，待扣回归零。实际页面排版已目视检查。
7. 修复弹窗关闭动画中标题切换的问题；保留已关闭表单模式，避免成功后短暂变成另一类表单。

期间统计使用原收款日期与退款/冲销发生日期；余额采用当前净额。原分成计划变化不改写已计提快照，实际支付与计提调整分开。报表收款明细标注负数更正说明，流水导出补充已确认退款/冲销。合同金额统计尚待合同变更批次统一接入。

## 复核记录

- 结构：系统临时目录 `lawlink_workflow_trial_20260919_96b87b72.json`。
- A 回归：`registration-dd7ebf72.json`、`ledger-1427bba8-result.json`。
- B 服务：`correction-7a97aea2.json`；复现脚本 `scripts/verify-workflow-corrections.ts`。
- 独立预览关闭后，已只读确认预览账号停用、主库没有新财务结构。主站 `localhost:3000/finance` 已实际打开，登录会话有效；主进程工作目录仍为本仓库。
- 未清空主业务数据、未执行主库迁移、未更改账号密码、未发布。主库切换需待新流程全部就绪，再展示清理与迁移脚本、影响范围及验证结果。

| 文件 | SHA256 |
|---|---|
| `prisma/schema.prisma` | `a8e4a028f6d9a6d334aa7473352f5adb9942752fcfcb7750a1ce1e98f28330b1` |
| `prisma/workflow-finance-proposal.prisma` | `4bdfec01d6f276cb87fca682689551a65a2fec5bc7f97b7aec9f26ee30dedf6f` |
| `docs/BUSINESS-WORKFLOW-FINANCE-MIGRATION-20260919.sql` | `1442c4edd5ed14d95c21265b2780466fa4b00d393091fa0675d7b2b1334dc4fa` |

---

以下为此前阶段记录，日期、库名与哈希仅对应当时版本；当前结论以上文为准。

# 财务 A 登记、确认与统一统计验收（全新数据）

新登记、签署分期、到账确认、支出、核销及票款关联已接入独立测试库，财务、案件、客户、工作台、报表及导出已接入统一事实服务。**本轮未清空本地业务数据、未执行财务主库迁移、未切换运行 Schema；退款、合同变更及实际分成支付仍属批次 B。**

## 当前测试基线

- 独立库：`lawlink_workflow_trial_20260919_f91005d6`，结构验收完成于 `2026-09-19T14:55:17.531Z`。
- 基线包含同期另一项工作加入的独立律师岗位和个人管理授权字段；财务 SQL 不包含这些岗位变更。前一个 `1b8581db` 测试库因个人管理授权字段更新而停止使用，重新建库验证，不在旧测试结构上补凑结果。
- 从现行空模型初始化，再执行财务 SQL、写入合成样本；5 张新表、10 条 CHECK 均生效，Prisma 与候选结构比较无差异。
- 43 项数据库约束用例通过：8 项合法写入、35 项按指定错误类别拒绝。
- 28 项真实登记服务用例通过：草稿不产生应收、完整分期签署、零收费、条件确认、收案费用仅带入草稿、支出独立、来源同案同类、普通经办及纯系统管理员不能确认、任何人登记都待确认、并发确认唯一、分成比例/基数快照、退回原因与通知、审计失败整体回滚、统计范围隔离。
- 18 项分配服务用例通过：四万再六万、累计分配、票款独立、跨案/越权/停用/归档拒绝、同版本并发仅一次、重复版本拒绝、审计失败回滚、性质与来源一致性。

## 页面验收

在独立预览中实际操作案件 `UI-823f893d`：

1. 登记六万元，页面只出现待确认，实收仍为原四万元；确认到账后实收变为十万元。
2. 分别核销四万元、六万元，应收余额依次为六万元、零元。
3. 对已核销款再关联一万元发票，十万元发票显示已关联一万、未关联九万，应收余额仍为零。
4. 新增一百元已签署收费及条件分期，生成一百元应收；填写条件成就依据后从“条件待成就”进入到期日期计算。
5. 登记 12.34 元支出，在财务流水显示付款，不增加实收或生成分成。
6. 财务主页按律师费显示实收统计；流水标明律师费、代收款、代垫回收及其他款项；已入账记录不再标为“可更正”。不把无明确到期日的应收宣称为均在账期内。

独立预览已关闭，合成预览账号已只读确认停用；主站监听进程属于本仓库，登录后财务页已实际打开验证。

页面已检查实际布局及交互。数据权限、跨案、并发与事务失败由真实数据库用例验证；未把每一个客户/报表/导出消费路由都算作独立浏览器验收。

## 登录故障处置

验收期间主开发进程驻留旧 Prisma 客户端，新增 `managerAuthorized` 字段触发 `Unknown field`，登录表单误报密码错误。只读核实账号正常、未锁定、无最近密码错误记录后，重启本地主服务加载新客户端，用户已重新登录。未改账号密码。登录页面现区分凭据拒绝与服务异常，不回显底层错误。

## 验证及复核文件

- 工程验证：82 个测试文件、647 个用例通过；ESLint、TypeScript、现行/候选 Schema 校验通过；生产构建通过。
- 约束结果：系统临时目录 `lawlink_workflow_trial_20260919_f91005d6.json`。
- 登记结果：同目录 `registration-86c20f65.json`；分配结果：`ledger-14209b3f-result.json`。
- 可复现脚本：`trial-workflow-finance-migration.ts`、`verify-workflow-registration.ts`、`verify-workflow-ledger.ts`、`preview-workflow-ledger.ts`，均位于 `scripts/`；仅使用独立测试库。

| 文件 | SHA256 |
|---|---|
| `prisma/schema.prisma` | `a8e4a028f6d9a6d334aa7473352f5adb9942752fcfcb7750a1ce1e98f28330b1` |
| `prisma/workflow-finance-proposal.prisma` | `e212485fd6023c93b8346e19cebab4210e3c93ccf5816810af9c93f59e1db61e` |
| `docs/BUSINESS-WORKFLOW-FINANCE-MIGRATION-20260919.sql` | `095b18ba1af4382771a4ede4140398b66b03a94d57c0791084ab2e1fbb7869fe` |

## 切换边界与下一批

运行模型尚未切换，原业务库继续使用未迁移时的入口；新模型就绪后写入旧入口会被阻止或转入新服务。这是发布阶段隔离，不是旧账补录或历史转换功能。主库清理及新模型切换仍须准备具体脚本、影响范围和复核结果后另行确认。

下一批实施退款保留/免除债权、合同总价追加/调减/替代、原分成追溯及实际支付/扣回，再交付统一切换。归档财务收尾另行验收，当前仍禁止归档案件财务写入。

原 `f1846e01`、`1b8581db` 及更早试验库保留，不复用旧试验结论替代当前基线。本轮没有旧账转换、待核降级或生产操作。

---

以下为先前版本的历史试验记录，其 SQL 哈希、14 条约束、默认待核及 41 项测试结论不适用于当前新版。

# 历史试验：含旧账待核的初版（已被下述新版替代）

结论：**通过独立测试库的结构迁移和约束验证**。已原样执行审阅 SQL，无需修改该 SQL；不代表 A/B 业务功能已完成，也未升级当前业务库。

## 执行范围

- 授权：2026-09-19 用户明确要求“在独立测试库初始化现行模型并试执行该 SQL”。
- 成功测试库：`lawlink_workflow_trial_20260919_91ec8635`，完成于 `2026-09-19T13:32:13.683Z`。
- 从 `prisma/schema.prisma` 生成空库建表 SQL，在新库初始化，再写入合成记录，最后执行 `docs/BUSINESS-WORKFLOW-FINANCE-MIGRATION-20260919.sql`。SQL 自带 BEGIN/COMMIT，整体提交成功。
- 未复制客户或案件数据，未运行 seed，未发送通知。测试账号停用且无有效登录密码。
- 当前业务连接仅用于读取数据库名称、检查测试库名不存在、创建独立库；建表、样本和迁移均通过显式测试库连接执行。之后只读确认当前业务库没有本次五张新表。
- 运行 Schema 和客户端未切换；未把 SQL 放入正式迁移目录。SQL 头部保留原审阅标识，其“尚未执行”是生成时状态；本报告记录独立试执行，当前业务库仍未执行。

## 验证结果

| 项目 | 结果 |
|---|---|
| 原记录保留 | 合同、应收、实收、流水、核销、更正、发票七类合成记录，现行客户端可读取的全部字段逐值一致，包含金额、状态、来源和时间 |
| 不自动补账 | 无 Payment 的已确认实收仍保留原状，未自动补 Payment 或分成 |
| 存量默认状态 | 四类财务记录为 UNKNOWN / PENDING_REVIEW、版本为 0；历史更正为 LEGACY_REVIEW |
| 结构 | 5 张新表存在；数据库与候选模型的 Prisma 结构比较无差异，包含新增枚举、列、外键和索引 |
| 手写约束 | 14 条 CHECK 全部存在且已验证；通过真实写入验证其关键边界 |
| 有效写入 | 12 项通过：同案部分票款关联、解除核销后调整/退款、有效到期日期、四类完整核对依据、有效更正对象/确认/执行明细、真实分成支付 |
| 无效写入 | 29 项按预期拒绝，逐项校验 PostgreSQL 错误类别，不把任意报错算作通过 |
| 回滚 | 每项约束用例独立事务回滚，事后七类原字段仍相同，五张新表无测试明细残留；原始合成样本保留 |
| 工程检查 | 新脚本 ESLint、全仓 TypeScript 类型检查通过；本次未改应用实现，未重复上一轮构建和页面验收 |

29 项拒绝场景涵盖：发票/实收跨案、重复票款关系、零额关联、票款超额撤回、重复实收来源、不存在的来源流水/合同、重复应收来源键、合同自引用、应收调整后不足已核销、退款侵占核销余额、核销超额撤回、到期日期/条件/确认人缺失、四类无依据核对、更正缺对象/目标不符/多个对象/缺确认凭据、重复执行目标、执行类型不符、负数退款、分成缺凭据/作废说明。

## 留存与复现

- 脚本：`scripts/trial-workflow-finance-migration.ts`。运行方式：`node --import tsx scripts/trial-workflow-finance-migration.ts`。每次运行只创建全新库，不复用、不重置、不删除；运行仍须具有独立测试库授权。
- 原始结果：`/var/folders/1n/wywk2z516dqgnxlvy3w16m_r0000gn/T/lawlink_workflow_trial_20260919_91ec8635.json`，含逐项断言。
- 建表 SQL 同目录 `lawlink_workflow_trial_20260919_91ec8635-baseline.sql`。临时文件可能被系统清理，本报告与验证脚本为仓库内留存依据。
- 首次库 `lawlink_workflow_trial_20260919_df1bbe04` 已初始化，但合成合同误用了不存在的 SIGNED 状态，在样本准备阶段停止，**未执行财务迁移**。脚本改用现行 ACTIVE 后在另一个全新库完成验收。两个库均保留，未擅自删除。

| 文件 | SHA256 |
|---|---|
| 现行模型 | `1fe5aa94e076bcc4ec6cf8a0b69d579b58e036e409a3141af3115dcb92bebfb4` |
| 候选模型 | `8e2af9aad4de0c9eb6c5d71899fbc8fbfcf52faf5d2db7becafa3bded1ee27df` |
| 实际执行的审阅 SQL | `bd89633bdb9d336f2b036980a4b24f8c894875845a9de359d6d3f232e176dced` |
| 空库初始化 SQL | `6fbeef256994960045e3017b9bc9b3fedfe3ca84501fefba1ecf601cc4f90100` |

## 验证边界与后续

本次验证从**现行模型**初始化，不是生产备份升级演练，也不覆盖历史迁移中未被 Prisma 模型表达的全部手写结构。合成样本保留不等于所有真实旧账均可迁移；正式升级前仍须重跑存量核对并另行批准。

跨记录金额汇总、同类款项、当前授权、事务并发、执行幂等和页面操作仍需 A/B 服务实现与端到端验收。数据库 CHECK 只约束本行，唯一约束不能替代余额锁定；本次未将这些后续要求标为已完成。下一步可依已批准方案继续 A、B 功能开发；当前业务库迁移、旧账转换与发布不在本次授权内。
