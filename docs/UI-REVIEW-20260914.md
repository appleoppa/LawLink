# LawLink 系统与界面审查（2026-09-14）

范围：v4 墨案重建完成后的业务端、管理后台全部页面与主要弹窗。方法：逐页 1440 / 390 截图走查 + 代码扫描（原生弹窗、原生控件、日期格式、重复入口、无入口能力）。

## 一、问题清单（按影响排序）

| # | 类别 | 问题 | 证据 | 处理 |
|---|---|---|---|---|
| 1 | 交互一致性 | 35 处使用浏览器原生 `confirm / prompt`：外观脱离系统；期限「人工调整」用两次 `prompt` 让律师手敲日期和原因，无格式校验、易误操作 | `procedure-content.tsx`、`users-view.tsx` 等 | 新增全局确认弹窗 `useConfirm`；期限调整改为带日期控件与必填原因的表单弹窗 |
| 2 | 选项平铺过多 | 收案向导把长枚举全部平铺成 chip：案件类别 8 项、代理程序 14 项、主体类型 8 项、诉讼地位最多 23 项，一屏被按钮占满 | 新建收案第 1、2 步 | 规则：≤5 项用 chip（是否反诉、收费方式等），>5 项用下拉 |
| 3 | 同屏重复主操作 | 顶栏固定「新建收案」与页头主按钮并存：案件列表出现两个「新建收案」；客户页同屏两个不同主按钮 | 案件列表、客户、冲突检索、审批 | 一屏一个主操作：页面把自己的主操作注册到顶栏，页头不再重复 |
| 4 | 能力无入口 | 后端已实现、界面入口在早期版本丢失：AI 文书审查（单文档）、元典类案检索 | `server/ai/review-document.ts`、`server/yuandian/cases.ts` | 文书材料行增加「AI 审查」；案件「···」菜单增加「类案检索」 |
| 5 | 命名不一 | 侧栏「收案」进入的是案件列表「待审批」页签；「待审批 / 待补正」看不出是收案 | 侧栏与案件列表页签 | 页签改为「收案待审 / 收案待补正」 |
| 6 | 日期格式混用 | 同一系统出现 `2026/9/6`、`2026-09-06`、`9月13日` 三种写法 | 25 处 `toLocaleDateString("zh-CN")` | 列表与详情统一 `YYYY-MM-DD`（上海时区）；`formatDate` 默认格式同步 |
| 7 | 控件风格不一 | 5 处原生 `<select>` 与墨案下拉并存 | 用户、角色、审批权限、快递、用章申请 | 统一为墨案输入框外观 |
| 8 | 筛选样式两套 | 保全 / 快递 / 律所文书用圆点 chip 筛选，其余列表用分段控件 | 三个资料页 | 列表类筛选统一分段控件 |

## 二、处理结果（2026-09-14）

| # | 状态 | 提交 |
|---|---|---|
| 1 | 已处理：35 处全部替换；期限调整改表单弹窗 | R1 |
| 2 | 已处理：收案向导 5 组长选项改下拉并两两同行；RadioChips 超 6 项自动下拉 | R2 |
| 3 | 已处理：12 个页面主操作归顶栏；顶栏默认「新建收案」按权限显示；财务工具栏重复「登记收付」移除 | R3、R7 |
| 4 | 已处理：材料行「AI 审查」、案件「···」菜单「类案检索（元典）」，均按配置显示 | R5 |
| 5 | 已处理：页签改「收案中 / 收案待补正」（「收案中」含草稿与待审批，第二轮更正） | R4、第二轮 |
| 6 | 已处理：21 个文件日期展示统一 | R4 |
| 7 | 已处理：原生 select 全局套墨案外观（稠密表单保留原生以支持空值） | R6 |
| 8 | 已处理：保全、快递、律所文书、通讯录筛选统一分段控件 | R6 |

附带修正：案件上下文头隐藏空值字段；日程「添加事项」弹窗与顶栏「新建任务」命名统一；用户表日期列不折行；删除程序确认去掉重复首句。

## 三、原则（已写入 AGENTS.md）

1. 一屏一个主操作，放顶栏；页头只放次要操作。
2. 选项 ≤5 用 chip，>5 用下拉；有搜索需求（案由、客户、管辖地）用可搜索下拉。
3. 禁止原生 `confirm / prompt / alert`；危险操作用 `confirmDialog`（红色确认按钮 + 后果说明），需要录入的用表单弹窗。
4. 日期统一 `YYYY-MM-DD`，时间 `HH:mm`，均按 Asia/Shanghai。
5. 已实现的能力必须有可达入口；按配置状态显示，不做假入口。

## 四、第二轮：页面布局走查（2026-09-14，按演示数据逐页截图）

| 类别 | 问题 | 处理 |
|---|---|---|
| 数据错误 | 案件列表「办理中」计数把已结案、已归档也算进去（显示 10，实际 8） | 计数改为 进行中 + 暂停 + 待受理 |
| 时间错误 | 客户端组件用运行环境本地时区格式化时间：服务器与浏览器时区不同即水合失败、时间错位（生产若部署在 UTC 容器同样会发生） | 新增 `lib/ui/sh-time.ts`，日程、案件详情、案件列表、财务、冲突检索、工作台等统一按上海时区计算日历日与时刻；`daysUntil` 不再修改入参 |
| 内容错误 | 非诉/顾问、审查起诉、行政复议程序套用诉讼环节（「起诉立案」「开庭审理」） | 新增三套环节预设 |
| 冗余 | 非诉/顾问案件显示「下次开庭 · 暂无排期」 | 替换为「服务期限」信号卡 |
| 冗余 | 已归档（只读）案件仍有上传材料、登记进展、新增程序；「已归档」徽标出现两次；程序标签与类别同名时重复（商事仲裁 商事仲裁） | 已归档隐藏写入口；徽标去重 |
| 冗余 | 研判笔记标题截取正文首句，下方又完整重复正文 | 标题改为「研判笔记 + 归属环节」 |
| 冗余 | 日程列表左侧时间与副标题时间重复；「开庭 · 开庭 · 第一次庭审」前缀重复 | 去重 |
| 不协调 | 团队只读视图仍是旧版纯文本布局 | 按案件详情结构重做（上下文头、程序链、侧栏团队/当事人） |
| 不协调 | 归档申请横幅用紫色，与归档青铜色体系不一致；归档列表所内案号紫色、日期折行 | 统一青铜色、表格改墨案样式 |
| 布局错误 | 案件列表案号折成 3–4 行、最近期限列被裁切；财务流水「方式/经手/日期/金额」逐字竖排 | 固定表格布局、窄列不折行 |
| 布局错误 | 期限预警说明写「T-7 / T-3 / T-0 / T+1」，与实际档位不符 | 改为「T-3 / T-1 / T-0 / T+1 + 规则建议提前档」 |
| 体验 | 无权访问的收案详情显示英文 Next 默认 404 | 新增中文 404（业务区 + 全站兜底） |
| 体验 | 侧栏「报表」对经办律师可见，点击却被重定向回工作台 | 入口按页面同口径判定 |
| 体验 | 个人设置只有一个分区却单独占一列侧栏 | 去掉单项侧栏 |
| 体验 | 审批列表开票事项标题只显示抬头；时间带秒且格式不一 | 标题「事项 · 金额 · 抬头」，时间统一 YYYY-MM-DD HH:mm |
| 体验 | 无合同金额时显示「¥73,000 / ¥0 · 回款 0%」 | 显示「未登记合同金额」 |
| 体验 | 短信摘要截断在「9月18日09」半截处 | 放宽长度并补省略号 |
| 体验 | 工作台趋势图以「万」为单位传入、提示框显示 ¥24 | 统一按元传入 |
