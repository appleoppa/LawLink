# v2.0.0 正式发布验证

日期：2026-09-21。业务基线 `597404b`，用户追加批准修复提醒测试后以新提交发布。相对基线只调整测试、包版本与发布文档，未改业务逻辑或数据库结构。保留 RC1 历史说明。

## 本次独立复测

- 103 个测试文件、775 项测试通过；lint、typecheck、Prisma 模型校验、生产构建通过。
- 首次复跑基线为 773 通过、1 失败：上海 23:00 后测试生成的开庭已过时，原断言未覆盖该提示。修复采用固定时钟并参数化覆盖前后两个时点，不修改业务提醒逻辑。
- PostgreSQL 16 独立空库成功应用全部 3 个迁移。
- 从 v1.3.2 迁移建立独立旧库，放入合成用户、案件、合同、收支和审计记录，再依次运行增量 SQL、两次 resolve、deploy。升级成功，Prisma 模型比较无差异。
- 升级后的两笔合成金额 300.00 / 20.00 保留，moneyKind 分别为 LAWYER_FEE / EXPENSE_RECOVERY，历史实收待确认状态 PENDING 符合脚本规则。
- 升级库存在全部 10 个非系统触发器；回滚事务内尝试删除合成审计记录被数据库拒绝（P0001），记录保留。
- 全新初始化逐表计数：84 个模型中 76 个为空。非空仅 User 1、CauseOfAction 1729、DeadlineRule 16、Document 22、StageTemplate 4、SystemSetting 1、DocumentTemplate 22、SealTypeConfig 5。Document 全部为内置空白模板，业务模型为空。
- 不复制真实 .env、原工作区数据库、附件或备份；验证数据只在本机临时库和临时目录，未加入 Git。

## 验收证据边界

升级路径、守卫及收案至归档/借阅的业务流程实测由本轮验收记录和用户确认提供。本次发布独立复测覆盖上述自动检查、数据库升级及初始化，不冒充重跑完整浏览器流程。主库迁移零待办为用户提供的验收状态，本次没有操作主库。

多用户并发、OCR 可信度边界、法院短信平台全面兼容、借阅到期真实时间等待及完整云部署/灾难恢复均不宣称已覆盖。部署方升级前应按文档在自己的副本演练。
