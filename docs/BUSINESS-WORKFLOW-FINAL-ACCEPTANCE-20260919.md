# 业务流程修复：最终开发验收与本机切换清单

**结论：已批准的批次 0、A、B、C、D、E 及审批执行终止已实现，独立库业务验证和本机切换演练通过。主业务库已按最终批准完成切换，新模型已在本机主站启用。** 顾问续约/成果交付依已批准方案留待后续，不列入本次修复。

本报告替代此前阶段报告中“A/B 未完成、C/D/E 未实施”的当时状态。先前记录保留用于追溯，不能当作当前完成情况。

## 已实现的业务结果

| 范围 | 结果 |
| --- | --- |
| 票款与即时提醒 | 票款按金额关联；新增/改期/确认及时检查，到期队列才执行；发送前复核当前事项，取消或办结失效旧提醒 |
| 财务登记与统计 | 收费签署生成分期应收；所有实收先待确认；到账后生成实收、分成快照；应收核销与票款关联独立；财务、案件、客户、工作台、报表及导出读取统一事实 |
| 财务更正与合同 | 退款明确保留或免除债权；折让、误录冲销、分成实际支付/扣回留痕；合同追加/调减/替代总价/范围及期限变更/终止分别处理，不重复累计；已出票红冲/作废/换开不冒充退款 |
| 收案与冲突 | 完整补正、撤回再送审、冻结逐次申请与附件；转案复核；正式案件主体/角色/委托范围变更触发新复核；历史审批资格只开放当次申请材料 |
| 责任与交接 | 事项有责任人和承接状态；改期/取消/办结留理由与审计；普通交接须接收，独立应急权限可接管；停用人员/岗位持续提示未交接风险；收案紧急事项转案不断链 |
| 归档与执行终止 | 服务、程序事项、财务与卷宗分别核对；未结财务须指定有独立权限的负责人；原归档包冻结，后续补充另行审批；终止申请经另一名有权人确认，待确认阶段暂停实际开票/盖章 |
| 全新数据 | 无旧账补录、历史待核或旧账转换流程；新演示脚本不清理数据、不修改账号/配置，采用正式财务登记服务，实收保持待确认 |

新能力入口位于既有案件、收案、财务对账、日程及审批页面。案件责任面板归入办案进程，冲突与归档核对归入案件档案。系统管理身份不自动取得业务执行权。

## 验证证据

- 全仓 **83 个测试文件、651 项用例通过**；ESLint 无错误/警告，当前运行客户端和候选新模型生成客户端均通过 TypeScript 编译检查。
- 生产构建 `npm run build -- --webpack` 通过；候选 Prisma 模型有效。未更改构建配置，也未用忽略错误通过检查。
- 最终结构库：`lawlink_workflow_trial_20260919_b58e53a9`。从现行空模型初始化，原样试执行审阅 SQL；**13 张新表、21 条 CHECK**，Prisma 可表达结构无差异；**43 项有效/无效写入验证**（8 允许、35 拒绝）。
- 同库 **165 项真实业务断言**：登记/确认 28、核销及票款 18、更正/结算 36、合同及支出 16、收案 18、交接 14、归档/终止 20、边界 15。覆盖权限撤回、跨案/超额、版本变化、重复/并发、审计失败事务回滚。
- 最终提醒回归：独立库 8 项真实数据库断言，测试对象和通知全部事务回滚；队列 8 个时间/状态边界只读验证通过。
- 完整本机切换演练库：`lawlink_workflow_trial_20260919_cded5111`。**10 项演练核对通过**：清理、安装新模型、保留对象逐行摘要一致、模板原文件与账号通知保留、事项初始状态与承接提醒、切换前入口写入/新模型拒绝、正式服务生成新样本、重复生成无残留。详见 `BUSINESS-WORKFLOW-LOCAL-CUTOVER-TRIAL-20260919.json`。
- 浏览器实际操作（独立合成库）：四万再六万登记/确认/核销、票款单独关联；退款一万及分成支付/扣回；收案完整补正/正式检索/重新送审；事项承接、取消开庭、当日收案紧急事项在日历显示；审批终止申请权限拒绝、另一人确认、终止后隐藏执行入口；案件冲突复核与归档核对入口。已目视检查页面布局。
- 浏览器验证与服务验证区分：合同变更全组合、归档送审至打包的完整链路主要由数据库服务验证覆盖，未宣称每一种排列都完成浏览器逐步操作。主库切换后已用生成的演示样本复核实际运行入口，结果如下。
- 独立预览服务已关闭，关联合成账号已停用，数据库及样本保留供复核。主站 3000 仍正常登录，账号密码未改动。

完整结构及业务报告位于系统临时目录；固定结果与切换清单存入本仓库文档。复现入口为 `scripts/trial-workflow-finance-migration.ts`、`scripts/verify-workflow-*.ts` 和 `scripts/prepare-workflow-cutover.ts --trial`。业务验证脚本可用 `WORKFLOW_TRIAL_DATABASE` 指向已验证的独立测试库。

## 本机切换审阅清单（已批准并执行）

审阅对象：

- `BUSINESS-WORKFLOW-LOCAL-CUTOVER-20260919.sql`：明确清理与迁移合并在一个事务中；目标数据库固定为本机 `lawlink`，已经存在新流程则拒绝重复清空。
- `BUSINESS-WORKFLOW-LOCAL-CUTOVER-IMPACT-20260919.json`：只读生成的逐表影响和保留数量。
- `BUSINESS-WORKFLOW-LOCAL-CUTOVER-TRIAL-20260919.json`：同一清理与迁移内容的独立演练结果。
- SQL SHA256：`623d416d5925de89718fd05c274b95d4b144a9ee287a700e547e02c38b09abfd`。

当前清理范围：**10 个模拟案件、8 个模拟收案、13 个模拟客户**，及其事项、当事人、财务、审批、材料数据库记录、业务通知和瞬时队列。清理后生成 **3 个演示案件、1 个演示收案、1 个虚构客户**；一笔四万元收款保持待确认，不伪造已到账。

保留 **8 个账号**、密码与身份资料、岗位/团队/权限、审批配置、系统配置、字典、22 个模板及模板原文件、律所资料、公告、外部联系人、审计及外部调用日志。磁盘附件文件不物理删除，正式生产环境不涉及。

批准后执行顺序：停止本机写入并备份；重核当前模型和影响；执行上述完整事务；事务成功后登记对应迁移已应用、将已验证候选模型设为运行模型并重新生成客户端；用明确指定的现有承办账号生成全新演示样本；启动服务并复核登录、权限与业务页面。若影响扩大或保留对象校验失败，停止；迁移失败则事务整体回滚，不留下半清空状态。

本步骤依据 `AGENTS.md` 业务流程修复批次：“实际迁移和存量转换仍须展示 SQL/脚本、影响和核对结果后另行确认。” 用户已批准清理模拟数据；**叶森随后明确回复“批准本机切换”，本步骤已完成**。不包含提交、推送或生产发布。

## 已批准执行中的页面收尾

主站验收发现待确认收款表误用了票款核对表头；恢复为日期、案件、付款方、登记金额、登记人、操作六列。新演示样本须与正式建案一致，同时保存主要客户与案件客户关联，确保客户来源统计显示三个关联案件；仅修正本次新建样本，不引入历史数据修补流程。

同时修正报表页对异步路由参数的读取：先等待参数，再解析统计期，避免点击本月/本季仍落回年度统计。主站通过实际期间切换复核。

切换后，`prisma/schema.prisma` 已是新运行模型；旧结构仅作为迁移复现夹具保存在 `prisma/workflow-baseline-20260919.prisma`，独立试迁移脚本读取该固定夹具。它不参与应用生成，也不提供旧业务数据兼容入口。切换前准备脚本在主库已启用新流程时直接拒绝，防止重复准备清理。


## 本机执行结果（已完成）

- 已原样执行批准 SQL，2026-09-20 00:48:25 UTC 提交（北京时间 09-20 08:48）。清理与迁移同一事务成功；迁移历史已登记，数据库与运行模型比较无差异。
- 迁移前备份：`backups/workflow-cutover-2026-09-20T00-47-41-753Z/database.dump`，619747 字节，备份目录可正常读取。账号密码等仅保存在私有备份中，未写入日志或版本文件；磁盘附件保留。
- 22 类保留对象在迁移后、生成样本前逐行摘要一致；浏览器验收后，20 类账号/权限/配置/模板等对象仍完全一致；审计及调用日志允许正常追加。
- 已生成 3 个演示案件、1 个收案、1 个虚构客户；8 个原账号和22个模板保留。有效应收 13 万元、实收 0、待确认收款 4 万元；草稿合同 1.5 万元不生成应收。客户已关联3个演示案件。
- 主站实测：原登录会话有效；财务及应收分配、案件详情、收案详情、日程、报表正常；一次演示事项承接已保存并经数据库复核；月度报表筛选及导出参数同步，待确认金额未计入实收。原账号岗位没有被自动扩大权限。
- 切换后 651 项测试通过，ESLint、类型检查、模型校验、生产构建通过。本机仍使用开发服务，定时任务沿原开发模式不外发通知；生产发布不在此次范围。
- 结构/金额/保留范围结果存 `BUSINESS-WORKFLOW-LOCAL-CUTOVER-RESULT-20260919.json`。服务地址 `http://localhost:3000`。
