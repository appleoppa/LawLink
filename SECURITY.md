# Security Policy

LawLink 面向律师业务数据，默认把案件事实、客户信息、附件、财务记录和审计日志视为敏感数据。

## 支持范围

当前安全修复优先覆盖默认分支和最新公开版本。早期版本不承诺长期安全维护。

## 依赖审计状态

核对日期：2026-09-13；范围：v1.3.2 的锁文件。`npm audit`（包含开发依赖）与 `npm audit --omit=dev` 均报告 **0 个已知漏洞**。CI 对完整锁文件运行审计，新告警会阻止检查通过。

v1.3.1 的生产依赖审计曾有 9 个受影响包；修复前全量审计有 14 个。本版在现有主版本内升级 Next.js 至 16.3.5、NextAuth 至 4.24.15、PostCSS 至 8.5.28、Vitest 至 4.1.11，并更新 sharp、XML 解析及其他传递依赖。请使用本版锁文件执行 `npm ci`，不要只修改版本号而保留旧安装目录。

审计数量是当日已知公告的包级汇总，不代表不存在未知漏洞，也不等同权限审计、渗透测试或生产环境验收。后续安装和升级须重新审计；不能沿用此日期的结果。依赖升级的回归结果以对应提交的 CI 为准。

官方修复说明：[Next.js 安全更新](https://nextjs.org/blog/august-2026-security-release)、[NextAuth 4.24.15](https://github.com/nextauthjs/next-auth/releases/tag/next-auth%404.24.15)。

## 公开版本的数据范围

公开版不包含开发者本地模拟测试案件、业务数据库、附件或备份。初始化仅创建管理员和系统字典、模板、规则，不创建案件、客户、当事人、收案、财务或开票业务记录；CI 在空库中验证此约束。源码自动测试与设计稿中的虚构样例不会载入业务数据库。已有部署升级不会清空其原有数据。

## 报告安全问题

请不要在公开 issue 中披露可利用的漏洞、密钥、真实案件数据或复现细节。

推荐方式：

1. 使用 GitHub 的私密安全报告功能（Security Advisories / Report a vulnerability）。
2. 如果仓库尚未开启该功能，请先创建一个不含攻击细节的普通 issue，标题写 `Security contact request`，等待维护者给出私密沟通方式。

报告时请包含：

- 影响范围。
- 复现条件。
- 可能造成的后果。
- 你建议的修复方向。

请不要包含真实客户资料、真实案件材料、真实密钥或未打码的个人信息。

## 部署方责任

自部署使用者需要自行负责：

- 更换 `.env` 中的所有默认值。
- 妥善保管 `NEXTAUTH_SECRET` 和 `STORAGE_ENCRYPTION_KEY`。
- 配置 HTTPS、反向代理、防火墙和数据库访问控制。
- 定期备份数据库与 `storage/`，并单独备份文件加密主密钥。
- 控制管理员账号和服务器访问权限；系统管理、业务岗位、事项审批分别授权。
- 新建人员需按本所制度核对证件采集与照片留存目的、查阅范围和保管安排；视觉识别不是实名认证。
- 外部 AI/检索等服务可能接收输入内容，核对服务域名、数据范围和费用，不能把“自部署”理解为所有处理均不出本机。
- 默认 Docker 运行镜像未带备份脚本和 PostgreSQL 客户端，应配置宿主机备份并检查结果；定时任务注册和 `/api/health` 正常均不能证明备份可恢复。

如果 `STORAGE_ENCRYPTION_KEY` 丢失，已加密文件无法恢复。
