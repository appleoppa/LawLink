# Changelog

本项目遵循面向使用者可读的变更记录格式。版本号在正式发布前可能不完全遵循语义化版本。

> **升级前必读**：自 v0.48 起的数据库迁移**不可逆**（v0.48 删除了旧的 `Preservation` 单表模型）。
> Prisma 迁移没有回滚脚本，升级后仅切换代码无法退回旧版本。
> **升级前请先运行 `./scripts/backup.sh`**，这是唯一可靠的回退手段。
> 想继续使用旧版本的，见 [v0.47 tag](https://github.com/lawflow-boop/LawLink/releases/tag/v0.47)。

## [1.4.0] - 2026-09-21

> **⚠️ 从 v1.3.x 升级必须先执行一次手工标记，否则升级中断：**
> ```bash
> npx prisma migrate resolve --applied 0_init
> npx prisma migrate deploy
> ```
> 本版把迁移链重建为单一基线 `0_init`（旧链因 `2026*` 与 `v4*` 命名混排，在空库上无法从零重放）。既有部署的迁移记录里没有 `0_init`，直接 `migrate deploy` 会尝试重建已存在的表并报 `P3018 / 42710`，**且失败会写入迁移表并阻断后续全部迁移**。完整步骤与失败恢复见[升级说明](./docs/RELEASE-GUIDE-v1.3.md#从-v13x-升级迁移基线重建必读含手工步骤)。全新安装不受影响。

### 界面

- **v4「墨案」界面全面重建**：登录页、工作台、案件列表与案件档案、冲突检索、审批工作台、财务、日程、管理后台按效果图逐页重做，全站旧色值收敛为墨案 token；窄屏收口与留白统一。
- 案件详情页头「···」菜单收敛为页面级编辑与程序轴生命周期动作，不再重复常驻入口。
- 对账页九区块改为四个页签分区，长表默认折叠。

### 新增

- **提醒送达台账**：提醒从「创建即完成」改为可对账——对象 × 档位 × 通道 × 上海日 × 接收人一行，记录应发/实发/结果，作废矩阵随期限改期、确认、交接同步失效。投递器复核对象现值后才发送，杜绝投出已失效的提醒。
- **法定放假安排**：管理后台按年录入国务院放假通知（含调休上班日），期限届满日据此给出顺延建议（依据民诉法第八十五条第三款）。顺延为提示，不自动改写律师填写的到期日。
- **案卷借阅**：已归档案件的检索、借阅申请、审批、限时只读查阅与归还，全程留痕。
- **法院短信收件与阅读**：原文即时保存、私有来件暂存、一链多文件全量取件、后台队列取件与人工接续；OCR 与文书结构化分析形成整理建议，由律师逐条确认后才写入案件（B1/B2/B3）。
- **诉讼时效与举证期限**纳入预置期限规则库。
- 桌面连接器受控取件（一次性短时令牌，取件即审计）。
- 案件卷宗打包导出、本期应收核销率、收款按最早到期自动填满、低风险任务批量办结。

### 修复

- **提醒可靠性**：保全提醒补扫（此前当日停机即永久跳过该档）、接收人在职校验（此前投递给已停用账号即静默丢失）、邮件摘要按人聚合（此前全局 500 条截断，丢弃的恰是当天较晚产生的紧急项）。
- **Docker 部署链**：修正 compose 文件非法（`mailpit` 误置于顶级 `volumes`，导致整个文件无法解析）、镜像缺 `bash`/`postgresql-client`/`scripts` 目录、备份目录未挂卷、备份脚本与应用的存储路径变量名不一致。
- **审计可读性**：审计动作按词拼装，缺一词整条降级为「其他操作」——全仓 199 个动作中 48 个命中，含证件号明文查看、财务更正、二次验证变更等敏感操作。一次补齐 49 个词条与 10 个对象标签，并加元测试防回归。
- **检索完整性**：全局材料检索与案卷借阅待审列表的取数窗口早于权限过滤，导致本人有权查看的条目可能被无关条目挤出窗口而不可见。
- **冲突检索**：名称匹配改为双向并加归一化（组织形式后缀、全半角、地域前缀），此前录入详略不同即漏检。
- **时区**：报表窗口、月桶、编号年份、导出边界、日历计算等按上海口径归一，不依赖容器时区。
- **AI 外发可追溯**：外部调用台账补记发起人（此前该列恒为空），收案阶段材料的 AI 审查补合规留痕。
- **提示词注入防线**：AI 结构化输出补数量与长度上限，防单份文书产出海量建议；字段写入通道的双重白名单与律师逐条确认不变。
- 期限规则重算的不一致提示不再被字符串截断吞掉；保全到期日以法院载明为第一事实。

### 安全

- 升级 `nodemailer` 至 10.x 并在 `overrides` 固定，消除 2 项 high 级公告；`npm audit` 全量与生产依赖均为 0 个已知漏洞（核对日期 2026-09-21）。
- 出站请求补私网校验与连接钉扎，逐跳复验重定向。

### 本版不包含（如实声明）

- 提醒投递器的**多实例租约与持久退避**：单进程重入守卫已落地，本系统定位为单体单进程部署；多实例场景需 Schema 迁移，迁移 SQL 已备待审。
- 法院短信的**原文逐页回看**：字段已预留，尚未实现。律师确认整理建议时暂无法在界面内对照原文页。
- 开庭改期更正、环节级归类、分诊权限通道的界面入口。
- 未覆盖的验证面：写操作完整闭环的浏览器走查、多用户并发事务实测、OCR 识别可信度边界、法院平台取件兼容性（缺真实样本）。

## [1.3.2] - 2026-09-13

- 修复直接及传递依赖的已知漏洞，保留现有主要技术栈；当日生产与全量依赖审计均为零已知告警。
- CI 新增全量依赖审计，以及全新安装后八类业务表为空、材料仅允许内置空白模板的检查；保留必要管理员、字典与模板初始化。
- 公开说明明确不附带本地模拟案件、附件或数据库备份；排除本地测试输出目录。
- 不包含下一版在途功能，不修改数据库结构，也不清空本地测试数据。

## 1.3.1 — 2026-09-13

### Fixed

- CI 的模型比对改用独立影子数据库，避免污染全新安装测试库并触发 P3005。保留完整迁移、初始化、查询、测试与构建检查。

### Documentation

- 更新 README、安装升级指南、安全说明和版本使用说明，明确管理后台、岗位、团队、事项审批、归档制度及身份资料边界。
- 修正容器首次 seed 的源码依赖步骤，说明默认镜像自动备份的限制及健康接口的核验范围。
- 标明历史设计、Word 安装副本及早期审计记录的适用版本；旧的“零漏洞”和“计划中”描述不作为当前保证。
- 保持 v1.3.0 业务功能与历史标签，不引入下一版在途开发，不改数据库模型或迁移 SQL。

## 1.3.0 — 2026-09-13

本次归集 2026 年 9 月 10 日之前尚未提交的开发；代码基准为 9 月 7 日的本地历史快照。不包含 9 月 13 日进行中的 P0/P1 改进与 v4 界面原型。

### Added

- 独立管理后台 `/admin`，分离系统管理身份、业务岗位和事项审批资格。
- 常设律师团队、团队案件只读访问、自定义岗位角色及内置岗位显示资料维护。
- 按事项审批权限组与统一审批工作台，集中查看待办、历史、本人申请及待开票/盖章环节。
- 个人基本资料与登录邮箱维护、多类型身份证件登记、证件照片加密存储及访问审计。
- 本所归档制度配置、送审材料快照、逐项缺失说明和导出完整性核对。
- 云服务器安装说明及上述模块的设计、迁移和测试资料。

### Changed

- 立案审批完整展示申请资料、主体身份、冲突命中与处理历史；审批窗口和常用表单统一布局。
- 日历订阅入口移至日程页；上传提示、状态和权限相关显示统一中文。
- 旧系统设置页面迁至管理后台；个人设置保留本人资料与登录安全。

### Fixed

- 发布验证补修：让生产构建包含定时任务模块，修复启动时 `ERR_MODULE_NOT_FOUND`；保留开发环境不运行定时任务的规则。此项为版本归集时发现并修复的既有问题。
- 修正到期提醒日期、事项归属鉴权、客户可见性、分成比例与金额尾差处理。
- 阻止定时任务物理删除审计记录；补强开票号码、状态及归档案件开票校验。
- 补充岗位、团队、申请附件、归档和会话权限的回归测试。

### 升级说明

- 包含 7 份 `20260906*` 数据库迁移文件；发布代码不等于已在目标实例执行迁移。
- 本版采用新的管理与审批模型，不保留旧后台网址；升级须先备份并核对迁移 SQL、系统管理员、审批权限组与归档制度配置。
- 本次版本归集不操作任何运行实例的数据或生产部署。

### 本次验证

- 56 个测试文件、449 项测试通过；ESLint、类型检查、Prisma 模型校验及生产构建通过。
- 独立生产服务启动、定时任务注册及浏览器登录页检查通过；本次未连接业务数据库重跑登录后的全流程。

## 1.2

### Fixed —— 全新安装无法初始化（重要）

- **补上 v0.44 保全三层模型缺失的建表迁移**。`20260601_v044_preservation_three_layer`
  是一个空迁移，`PreservationCase` / `PreservationTarget` / `PreservationProperty` /
  `PreservationPropertyRenewal` 四张表的 DDL 从未进入迁移历史。
  **后果：任何全新安装在执行 `prisma migrate deploy` 时都会中断**（P3009，
  报 `relation "PreservationCase" does not exist`），无法完成初始化。
  已补 `20260601010000_v44_preservation_three_layer_tables`，其全部语句均为幂等，
  已在运行中的实例执行时为无操作，不会报错、不会丢数据。

- **补上 `Hearing.address` / `Hearing.contact` 两列的迁移**。这两列同样只存在于
  `schema.prisma` 而不在迁移历史中，导致全新安装即使完成初始化，一进工作台也会崩溃
  （`The column Hearing.address does not exist in the current database.`）。
  此问题由外部贡献者 **[@liuzl](https://github.com/liuzl)** 于 2026-06-26 首次报告并提交修复（PR #3）。
- **CI 增加 schema 漂移检查**。`prisma migrate diff --exit-code` 会在 `schema.prisma`
  与迁移历史不一致时使构建失败，防止再出现「用 `db push` 改库却没生成迁移」的情况。

### 法律准确性修正

- **更正财产保全期限的法条依据**。原引用「民诉法第 244 条」有误——该条实为执行回转，与保全期限无关。
  正确依据为《最高人民法院关于适用〈中华人民共和国民事诉讼法〉的解释》（2022 修正）第四百八十五条：
  冻结银行存款不超过一年、查封扣押动产不超过两年、查封不动产及冻结其他财产权不超过三年。
  年限数值本身一直正确，仅出处有误。
- **保全到期日改按日历年计算**。原实现用固定天数（365/730/1095），跨闰年会比法定期限早一天
  （如动产两年自 2027-06-15 起，旧算法得 2029-06-14，法定应为 2029-06-15）。现统一走期限引擎的日历口径。

### Added

- **保全续封提醒**。此前保全到期仅在日程与日历订阅中可见，不产生站内通知与群机器人推送。
  现按各保全案件自己的 `remindDays`（默认 30/15/7/3/1 天）提醒，到期当日与逾期首日一律提醒。
- **过期未续封的保全自动标记为「已到期」**。依据《最高人民法院关于人民法院民事执行中查封、扣押、
  冻结财产的规定》（2020 修正）第二十七条，期限届满未办延期手续的效力消灭。翻转时发送通知并记录审计，
  若实际已办续封可在系统中更新记录。
- **登录/登出审计**。补齐 `LOGIN` / `LOGOUT`，并记录 `LOGIN_FAILED`（区分账号不存在、已停用、密码错误）。
  失败原因仅进审计日志，不返回前端，避免账号枚举。

### Changed

- **案件详情页地址改用所内系统编号**。由 `/matters/cmq3lxgrm002nitzfhh8qn8ql` 变为 `/matters/M-2026-001`。
  旧的 cuid 地址仍然可用，会自动跳转到新地址，历史书签、通知链接、审计日志中的旧链接不受影响。
- **批量导入的案由校验**。导入此前是唯一绕过案由联动校验的写入路径。现会校验，但不合规时**降级为自由文本
  并正常导入**（历史案件按当时规则立的案由未必符合现行范围），并在导入结果中逐行标出待人工核对。
- **删除程序的确认提示改为显示真实数量**（「该程序下的 3 条期限、2 场开庭、5 个环节将被一并删除」），
  并更正原提示中「材料将被一并删除」的错误说明——材料不会被删除，仅解除与该程序的关联。

## 1.1

### Changed

- 案件详情页信息密度改造：信息总览重设计、摘要条滚动后出现、去除与页头/侧栏重复的内容。
- 财务面板紧凑模式改为单行展示；「合同额」更名为「合同约定律师费」。
- 日程月历移除底部当日详情模块。

## 1.0

### Added

- 内置文书模板由 10 个扩充至 22 个。

### Changed

- 外部联系人审核流默认关闭（改为设置项）。小型团队为信任环境，强制审批会让人放弃使用通讯录。
- `CommissionPlan` 自动分成降级为只读记录，不再在主流程中推送。

## 0.51

### Added

- 收件箱闭环：法院短信解析出的新案号可回填至 `MatterProcedure.caseNumber`；高置信度解析结果直接预填开庭/期限草稿。

## 0.50

### Added

- **ICS 日历订阅**。每位用户一个带 token 的订阅地址，开庭、期限、保全到期自动进入手机日历。
- **群机器人推送**。支持企业微信/钉钉 webhook，每日提醒汇总推送，未配置时静默跳过。
- **数据库备份 cron 化**。`scripts/backup.sh` 接入内置调度器，失败时通知管理员。

## 0.49

### Added

- **法定期限引擎**。新增 `DeadlineRule` 规则库，内置 15 条经元典核验的法定期限规则
  （上诉期、举证期、申请撤销仲裁裁决期限等），每条附法条依据与出处链接。
  期间计算依据《民事诉讼法（2023 修正）》第八十五条：期间开始的时和日不计入；
  按月/年计算时到期月无对应日的取该月最后一日。系统不内置节假日表，届满日为法定休假日时由界面提示人工核对顺延。

## 0.48

### Changed

- **移除旧的 `Preservation` 单表模型**（⚠️ 不可逆迁移），统一到 v0.44 引入的
  `PreservationCase → Target → Property → Renewal` 三层模型。
- Decimal 序列化收口到统一的深度转换，不再在每个 server action 中手写。
- 案件材料与程序环节改为外键关联（原先依赖字符串标签），环节新增 ACTIVE/HIDDEN 状态。
- 电子送达「待人工处理」状态改为独立列，可用 SQL 查询，不再需要全量拉取后在内存过滤。

## 0.47

### Added

- 案件详情页程序工作台重构。
- 法院短信电子送达附件提取；收件箱回归主导航。
- 商事仲裁案由与管辖机构的联动约束。

### Changed

- 工作区界面迁移至 v3 视觉风格。

## 0.1.0

- 初始公开准备版本。
- 收案登记、冲突检索、正式案件、案件跟进、财务记录、结案归档和数据导出主流程。
- Next.js App Router、Prisma、PostgreSQL、NextAuth.js、shadcn/ui 基础架构。
- 本地开发与 Docker Compose 部署配置。
- 私有附件存储、文件下载鉴权、基础审计日志和角色权限设计。

### Security

- `.env`、`storage/`、备份、构建缓存和本地输出目录默认不进入 Git。
- demo 案件 seed 默认不启用，避免真实或拟真业务数据进入公开仓库。
