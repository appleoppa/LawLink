.next
.next-dev
.next-build
node_modules
.npm-cache
.env
.env.*
!.env.example
storage/*
!storage/.gitkeep
coverage
dist
*.log
.playwright-cli/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# 设计参考素材（不入版本，本地保留）
截图/
参考/

# Prisma 本地数据
*.db
*.db-journal

# Backup
*.bak
backups/
backup*/
*.backup
*.dump
*.sql.gz
database.dump
storage.tar.gz

# TS incremental
tsconfig.tsbuildinfo

# Claude Code session notes（含临时凭据，不入版本）
.claude-sessions/

# Working scratch (outside of project scope)
outputs/
outputs/

# Local test and browser artifacts are not release content
output/

tmp/
tmp_case_import_preview.json

# PGG/LawLink sync tooling is tracked (scripts/*pgg*.js). It must stay free of real
# case paths, party data and operator identities: every environment-specific value is
# read from env and fails closed when unset. Only generated previews stay ignored.

# Accidental mojibake scratch files from local notes/exports.
u[0-9a-fA-F]*
