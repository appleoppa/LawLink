## LawLink 工作区规则

> 这是项目的"约束先行"文档。任何 AI 助手（含 Claude Code）或人类协作者在本仓库工作前，必须先读完本文件。
> 规则与实践冲突时，先改本文件、再改代码。

---

### 一、项目定位

- **LawLink** 是面向独立律师、小团队和小型律所的**开源、自部署**案件/项目管理系统。
- 第一版主线工作流：
  `收案登记 → 冲突检索 → 转正式案件 → 持续跟进 → 财务记录 → 结案归档 → 数据导出`
- **不做共享 SaaS 多租户**。一个律所/团队部署一套实例，单体应用 + 单库。
- 协议：**MIT**。

---

### 二、技术栈（已定型）

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 16 App Router + TypeScript | SSR + RSC，单仓单进程 |
| UI 组件 | **shadcn/ui** + Radix UI | 拷贝式组件，深度可定制 |
| 样式 | **Tailwind CSS** | 高密度浅色工作台，保留主题扩展能力 |
| 动效 | **Framer Motion** | 微交互、页面切换 |
| React | React 19 | Next.js 16 配套 |
| 图标 | lucide-react | |
| 图表 | Recharts | 工作台、财务统计 |
| 表单 | React Hook Form + Zod | |
| 表格 | TanStack Table | 表头筛选、排序、分页 |
| 数据库 | PostgreSQL 16 + Prisma 5 | |
| 鉴权 | NextAuth.js（Credentials Provider） | 邮箱密码登录 |
| 部署 | Docker Compose（app + postgres） | |

**已弃用**：Ant Design。不要再引入 `antd` 或 `@ant-design/*`。

**预留接入**（V1.5）：元典开放平台 MCP（law/case/company），用于冲突检索增强和企业核查。

---

### 三、目录约定

```
LawLink/
├── docs/                  # 设计文档（PRD / DATA-MODEL / UI-DESIGN）
├── prisma/                # schema、迁移、seed
├── public/                # 静态资源
├── storage/               # 本地私有文件（不入 git）
├── src/
│   ├── app/               # 路由与 Route Handler
│   │   ├── (auth)/        # 登录、注册路由分组
│   │   ├── (app)/         # 登录后的主应用路由分组
│   │   ├── (admin)/admin/ # 独立管理后台页面壳；仍复用同一登录、服务层与数据库
│   │   └── api/           # API 路由
│   ├── components/
│   │   ├── ui/            # shadcn/Radix 原子组件与全局无障碍、动效适配
│   │   ├── patterns/      # LawLink 跨页面视觉与交互模式
│   │   ├── layout/        # AppShell、Sidebar、Topbar
│   │   ├── matters/       # 案件相关组合组件
│   │   └── ...
│   ├── lib/               # 业务规则、工具函数、Prisma client
│   │   ├── auth/          # NextAuth 配置、session helpers
│   │   ├── permissions/   # 角色权限、案件可见性
│   │   └── ...
│   ├── server/            # Server Actions、服务层
│   ├── types/             # 共享 TS 类型
│   └── styles/            # globals.css、Tailwind config 扩展
└── tests/                 # （V1.5 引入）
```

**规则**：
- `src/components/ui/` 只放 shadcn/Radix 原子组件。允许在原子层维护全站统一的语义 token、无障碍和基础动效适配，但禁止加入 LawLink 业务结构或领域判断；更新 shadcn 时必须审计并保留这些适配。
- `src/components/patterns/` 放 `PageHeader`、`ListToolbar`、`FormSection`、全局动效策略等 LawLink 品牌化跨页面模式，不承载 Server Action 或领域状态机。
- 跨页面复用的业务组件提到对应领域目录，仅单页用的组件就近放在路由目录下 `_components/`。
- 业务规则（金额计算、状态机、冲突匹配算法）一律沉淀到 `src/lib/` 或 `src/server/`，禁止散落在页面里。

---

### 四、命名约定

| 对象 | 风格 | 例 |
|---|---|---|
| 路由目录 | 小写英文复数 | `matters/`、`intakes/`、`conflicts/` |
| React 组件 | PascalCase | `MatterCard.tsx` |
| 工具函数 | camelCase | `formatCurrency`、`computeReceivable` |
| Server Action | camelCase + 动词开头 | `createMatter`、`closeMatter` |
| Prisma model | PascalCase 单数 | `Matter`、`Client` |
| Prisma 字段 | camelCase | `caseNumber`、`createdAt` |
| 数据库表（自动） | snake_case | Prisma `@@map` 控制 |

**术语固定**：
- 收案线索 → `Intake`
- 正式案件/项目 → `Matter`
- 客户（机构/个人） → `Client`
- 客户联系人 → `Contact`
- 当事人/相对方/第三人 → `Party`
- 收/付费记录 → `FeeEntry`
- 结算单（合同/账单） → `Billing`
- 法定期限 → `Deadline`（带 `category`：诉讼时效/举证/上诉/履行/其他）

**界面语言**：面向用户的按钮、状态、指引、说明和无障碍提示默认中文；数据库/API 枚举须转换为中文标签后展示。品牌名、文件格式、必要的技术配置标识和用户原文可保留英文。文件上传控件须提供中文选择提示，不依赖浏览器系统语言。展示映射集中在 `src/lib/`，不得改变已保存枚举值。

**v4 墨案 UI 重建（2026-09-13 批准）**：界面以 `docs/mockup/v4/` 效果图为验收基准全面重建，不在旧页面上换色小修；方案与页面对照见 `docs/UI-V4-REBUILD-PLAN.md`。原子层 `src/components/ui/` 直接使用墨案 token（`src/app/moan.css`），不得再写 v3 硬编码色值；页面骨架与签名母版（PageHeader、Panel、SpineRow、RiskLadder、ProcedureChain、ReviewSeal 等）统一放 `src/components/patterns/`。重建不改 Schema、权限与 Server Action 语义，效果图未画到的旧模块须归位不得删除，不做无后端支撑的假功能。

**界面交互原则（2026-09-14 审查确立，详见 `docs/UI-REVIEW-20260914.md`）**：① 一屏一个主操作，由页面通过 `useTopbarAction` / `TopbarLinkAction` 注册到顶栏，页头与工具栏不重复同一主按钮；② 单选项 ≤5 用 chip，>5 用下拉（`ChoiceField`，`RadioChips` 超 6 项自动收为下拉）；③ 禁止原生 `confirm / prompt / alert`，确认用 `confirmDialog`，需录入的用表单弹窗；④ 日期统一 `formatDate`（YYYY-MM-DD）与 `formatDateTime`（YYYY-MM-DD HH:mm），上海时区；客户端组件禁止用 getHours()/getDate() 等本地时区方法做展示与日历计算，改用 `src/lib/ui/sh-time.ts`（否则服务器与浏览器时区不同会水合失败）；⑤ 列表筛选统一分段控件或 `FilterSelect`；⑥ 已实现能力须有可达入口，并按配置与权限显示；⑦ 案件详情页头「···」菜单只保留页面级编辑入口与程序轴生命周期动作（暂停/结案/归档），不重复常驻入口——财务与用印走吸顶页签、类案检索在办案进程、开庭经「＋事项」、新增程序在程序栏（2026-09-20 用户确认收敛）。**服务轴 UI 已整体移除**（2026-09-20 用户确认：概念传达失败且无任何下游依赖——结案、归档、报表、工作台均不读取 `serviceStatus`，「活干完等收钱」由结案态与财务待收本身表达）：档案页签「服务状态」区块与案卷头「服务已完成」徽标删除；`Matter.serviceStatus` 字段、`completeMatterService`/`activateMatterService` 及其测试保留，无 UI 入口即不生效，待未来统计确需区分「服务中/待收尾」时再启用，不为此走 Schema 迁移。

**自然人证件类型与校验（2026-09-14 用户确认，含数据库变更）**：客户与当事人共用 `ClientIdType` 证件类型——自然人可选居民身份证（默认）、港澳居民来往内地通行证、台湾居民来往大陆通行证、护照、外国人永久居留身份证、其他；机构为统一社会信用代码。`Party.idType` 可空，历史数据为空时按身份证处理。选择身份证时输入只允许数字与末位 X、自动转大写、限 18 位，位数不对不能通过（前端即时提示 + 服务端 zod 同规则）。有统一社会信用代码的主体输入名称时通过元典企业信息接口匹配，回填信用代码、注册地址与法定代表人（未配置时静默不影响手工录入）。审批中证件号码直接明文展示。

**冲突预检与正式冲突检索分离（2026-09-14 用户确认）**：系统中有两处冲突检索，定位不同。① **正式检索**在收案流程内：收案向导提交时自动携带全部主体检索并挂在该收案上，收案详情给出结论（可承接 / 有冲突 / 信息不足），收案审批核查，转正式案件前把关；未命中时系统记为「未命中（系统自动提示）」。② **工作区「冲突预检」**（顶栏「冲突预检」按钮、首页快捷弹窗、客户档案入口；2026-09-18 起不在侧栏占一级导航）是律师收案前自行了解本所记录：不经审批、**不出任何结论**（`ConflictCheck.intakeId` 为空、`conclusion` 恒为 PENDING，`setConflictConclusion` 拒绝预检记录）、不能挂靠或替代收案检索；页面按检索主体全面展示命中的历史案件与在办收案、命中方式、对方角色与风险提示，未发现不表述为无冲突。预检仍留存检索记录并写审计，但页面不再展示「既往预检记录」（2026-09-18 用户确认：只有主体名与命中条数、点不开、且很快过期，回看无价值，留痕由 ConflictCheck 与审计日志承担）。两处检索范围一致：历史案件当事人（含单位信用代码列）、客户档案关联案件（证件用盲索引比对）、以及尚未转为案件的在办收案（INTAKE / PENDING_CONFIRMATION / NEEDS_REVISION，正式检索排除收案自身）；在办收案命中只披露收案名称、登记人、状态、登记日期与命中角色，不返回收案 ID、不开放收案内容。③ **案件办理中的动态复核**（2026-09-20 用户确认展示位置）：新增程序、新增委托合同、补充协议生效三处写路径由 `assertMatterReviewCurrent` 硬门禁拦截，主体或代理范围变化后须先重新检索并给出排除冲突结论；案件详情页侧栏（各页签常驻，承办团队卡之下，2026-09-20 用户确认：不占主视觉）的「利益冲突复核」卡**仅在待复核时渲染**（从未检索、主体指纹变化、最新结论未排除冲突三种状态任一命中），平时不展示——「当前主体已核查」对律师无信息量（收案审批已有结论），历史核查记录不再常驻可见，留痕由 ConflictCheck 与审计日志承担。

**侧栏信息架构（2026-09-19 用户确认）**：单列不分组——工作台 / 案件 / 审批 / 日程 / 客户 / 财务 / 报表，条目不多，分组标签只会增加噪音（先后作废过「工作区 / 业务 / 资料」与「办案 / 经营」两版分组）。「更多应用」是工具抽屉不是导航项，固定在底部用户卡上方。**法院短信不进导航**：它是有时效的外部来件队列（送达回执、开庭传票），入口在顶栏与通知铃并排，图标带未处理角标（开庭 / 举证 / 缴费类标红），点开列未处理短信并可就地「标记已处理」或「去处理」跳 `/inbox?focus=<id>` 定位高亮；整页 `/inbox` 保留分诊能力。收案不占侧栏一级——`/intakes` 列表早已并入 `/matters?tab=intake`，「收案中」「收案待补正」是案件列表里与办理中同级的页签，新建入口在顶栏主按钮。冲突预检不占侧栏一级——入口在顶栏「新建收案」左侧的次级按钮（先预检再建案的动线）；归档不单列分类——「已归档」本就是案件列表与办理中、收案中同级的页签，归档台账（归档号、归档日期、结案原因、卷宗目录）作为该页签下的「归档台账」链接进入 `/archive`。团队只读视图与案件详情共用同一套墨案版式（案卷头 + 程序栏 + 主栏/侧栏），只换外观，可见内容与权限不变。

**案件详情页「案卷工作台」（2026-09-14 用户确认第三版）**：当事人区我方与对方**上下排列**（2026-09-19 用户确认：左右分栏时两侧信息量不同会高度不齐，很难看），中间用带「诉 / 辩」圆标的横线分隔。案卷头 → 程序栏（浅色程序卡片，点击切换，全页共用）→ 页签「案件档案（默认首屏：基本信息含当前程序字段、当事人紧凑卡片）/ 办案进程 / 委托与财务 / 审批用印」，侧栏（承办团队、最近待办、最近记录）在各页签常驻。办案进程按环节组织：左列圆形节点竖排环节（末尾「＋ 添加环节」，窄屏变横向；2026-09-19 用户确认去掉竖列首项「总览」，始终以选中环节为范围）+ 环节操作条 + 待办 + 材料（证据要点作为标签挂在材料行上，不单设证据链模块）+ 经办记录（已发生事项，含快递与备忘，不含材料）。**同一数据只在一处展示**。细则见 `docs/UI-MATTER-DOSSIER-PLAN.md`。视觉：深墨头版 + 压边程序卡 + 吸顶胶囊页签 + 柔和阴影区块；案件详情页不使用宋体（用户反馈显得廉价），标题统一无衬线粗体。

**开票与收款动线（2026-09-18 用户确认，含数据库变更 v44_fee_confirm_state）**：① 开票不以收款为前提——律师提交开票申请（关联案件必传开票依据，专票校验购方六要素），审批人可在批准时直接上传电子发票与发票号一步到 ISSUED，也可先批准（APPROVED＝待开票）再由具「执行开票」权限的人上传完成；电子发票加密入库并挂案件材料。② 实收一律两步，无人例外（2026-09-19 用户确认）：**任何人登记的实收都先记 `FeeEntry.confirmState = PENDING`**——经办律师、主任律师自己收的案件、财务本人登记的都一样——不生成实收 Payment、不派生分成、不写时间线、不计入已实收与回款进度、不进报表与工作台统计，只在财务页「待确认实收」与案件财务区以琥珀色呈现。确认（`confirmFeeEntry`）与退回（`rejectFeeEntry`，删除该条 + 写审计 + 通知登记人）**仅限持有「确认实收到账」（`finance.confirm`，仅 ALL 范围）的财务管理人员**：内置角色中只有「财务」自带，主任律师不再自动具备；其他人选由管理后台「岗位角色」配置自定义角色授予，人选与权限范围都在后台设置。确认人记在 `confirmedById/confirmedAt`，确认与退回均写审计。③ 发票与实收勾稽：登记收付时可选本案发票自动带出发票号与金额，财务页「开票管理」下的「已开票未收款」列出已开具但无对应实收的发票。

**案件一案一签，不设委托模块（2026-09-16 用户确认）**：收案登记的收费约定、委托合同与合同扫描件统一并入「案件档案 · 委托与收费」一个字段表（2026-09-18 用户确认：合同仅指委托代理相关合同，不另立「合同与材料」子框）；中途变更收费或增加代理程序＝在「委托与财务」新增一条补充协议（Billing），原合同保留、合同额合计，不改原合同也不新增模块。

**案件详情页的事项与记录（2026-09-15 用户确认）**：办案进程只有两个添加入口——「＋事项」（任务 / 期限 / 开庭，进待办）与「＋记录」（沟通 / 寄收件 / 研判，进经办记录）。「备忘」不再新增，等同于无截止日的任务；历史备忘作为事项显示在待办中，可办结、删除。快递不手写状态，填单号后由接口更新。完成的事项与系统事件自动进入记录。

**日历订阅入口**：统一放在日程页右上角，以「订阅日历」次级按钮打开弹窗，管理订阅说明、复制链接和重置链接；个人设置不再展示订阅卡片。弹窗组件就近放在 `schedule/_components/`，仅打开时获取订阅链接，沿用现有订阅权限与接口。

---

### 业务流程修复批次（2026-09-19 确认）

- 2026-09-19 最终执行授权：叶森明确回复“批准本机切换”，授权执行已展示并完成独立演练的本机清理与迁移 SQL（SHA256 `623d416d5925de89718fd05c274b95d4b144a9ee287a700e547e02c38b09abfd`），先备份，保留账号/权限/配置/模板及磁盘文件，再生成新演示样本并验收主站；不包括生产发布。
- 本机执行状态：上述授权 SQL 已提交，`prisma/schema.prisma` 已启用新模型，迁移记录已登记；账号/权限/配置/模板保留并经摘要核对，全新演示数据已生成，主站验收完成。旧模型仅保存在迁移复现夹具 `prisma/workflow-baseline-20260919.prisma`，不得重复执行清理 SQL。详见最终验收报告。

- 按 `docs/BUSINESS-WORKFLOW-REPAIR-PLAN-20260919.md` 的复核后方案实施：先固定在途改动基线，批次 0 优先修票款误判和即时提醒，A/B 共同设计后依次实施，C/D 统一收案与责任人约定后实施，归档收尾单独验收。顾问续约/交付为后续扩展；审批终止可独立推进。
- 第 2 节八项业务规则及第 12 节调整后 Schema/仅生成迁移 SQL 已获授权；实际迁移和存量转换仍须展示 SQL/脚本、影响和核对结果后另行确认。既有规则冲突时以本批确定规则为目标，未完成实现不得宣称已生效。
- 2026-09-19 追加授权：在独立新建测试库初始化现行模型，试执行 `docs/BUSINESS-WORKFLOW-FINANCE-MIGRATION-20260919.sql` 并验证约束；仅使用合成样本，不升级当前业务库、不转换旧账。测试库保留供复核，清理另行确认。
- 2026-09-19 后续修订：所有功能按未来全新系统实施，不开发旧数据兼容、历史补账、待核降级或旧账转换流程。叶森明确允许删除现有本地模拟业务数据并重新生成；账号、岗位权限、审批配置及系统基础配置保留。新数据必须从登记时满足新模型与业务规则。此决定替代本批关于保留旧账和旧/新口径并行的实施要求，不授权生产数据清理或公开发布。
- 保留一案一签、原合同及历史；补充协议按追加、调减、替代总价、范围/期限变更、终止结算分别计算，不把所有金额直接相加。正确回款指标按同一批应收核销计算，不再以年度总收款除年度新应收冒充回款率。
- 归档原包保持不变；后续经专门授权追加财务收尾及补充归档，不通用放开历史案件写入。财务更正确认和应急接管是独立权限，不随系统管理身份取得。
- 批次 B 的 `finance.correct`（确认财务更正）及 `finance.settle`（登记实际分成支付/扣回）均为独立 ALL 权限；内置财务具有这两项，其他岗位通过自定义角色显式授予。到账确认权限、业务管理权和系统管理身份不能替代。申请、确认、计提调整、实际支付/扣回分别留痕；未确认申请不改变余额。该流程仍在独立库验收，不能据此宣称主库已升级。
- 批次 0 的票号提示仅为过渡；新系统统一使用发票 ID 的金额关联。后续验收采用全新合成数据，不因旧账缺漏增加兼容流程。
- 提醒创建、改期、确认后及时评估，并定时补扫；同一事项日期/接收人去重，发送前核实当前状态。队列必须到执行时间才领取任务。

---

### B2/B3 实施记录（2026-09-20，法院短信阅读与确认层）

- **B2 阅读与建议**（迁移 20260920000004_sms_b2_suggestions 已执行：SmsSuggestion 表+SmsInboundFile 分析列+三枚举）：① OCR provider 抽象（`src/server/ocr/provider.ts`）——默认复用 AI 设置的视觉模型识别图片（多模态 aiChat），可选配置独立 HTTP OCR 网关（SystemSetting `ocrSettings`，声明 supportsPdf 可整体识别扫描 PDF）；管理后台「AI 与元典」页新增 OCR 设置卡；AI/OCR 未配置时文件降级 NEEDS_OCR 可见，不显示为分析成功。② 阅读管道（`src/server/sms/analysis.ts`）：文本层提取→无文本层 OCR→AI 结构化分析（文书类型/字段/开庭期限事项/案号）→与档案比对落 SmsSuggestion（DOC_TYPE/MATTER_MATCH/FIELD_CHANGE/HEARING/DEADLINE）；拒绝过的同内容建议不重复弹回；取件成功后自动入队 `sms.file_analysis`（worker 已注册）。③ B1 小项补齐：私有来件区独立页签（待匹配）、下载流式限制（readBodyWithLimit 覆盖谎报 Content-Length 的响应体）、页面候选超限完整性提示（前批已做）。
- **B3 确认与执行**（`src/server/sms/confirm-actions.ts` + 收件箱「整理建议」区块）：律师对建议逐项或批量确认；应用在 Serializable 事务内复核对象级权限（assertCanHandleMatter）与目标状态，FIELD_CHANGE 条件更新程序案号/法院并写时间线，HEARING/DEADLINE 建开庭与期限（generatedHearingId/generatedDeadlineId 幂等，默认责任落程序主办），MATTER_MATCH 更新归属并转正私有暂存文件；同批原子提交，单项失败整批回滚可重试；缴费类通知只建待办不自动记财务。收件箱来件卡展示：处理状态徽标、文件清单（含阅读状态与文书类型）、整理建议清单（采用/拒绝/全部确认）、人工接续补传。
- 降级验收：AI 未配置 → 文件 NEEDS_OCR、保存取件人工确认照常（单测 sms-analysis.test.ts 5 例覆盖）。**未做（如实声明）**：开庭改期更正（现按新开庭建立，旧开庭由律师手工取消——深化项随真实样本反馈迭代）；B2 的环节级归类（立案/举证/开庭环节匹配建议）与来源页原文回看定位 UI——文书页段表已预留（SmsSuggestion.sourcePage/sourceExcerpt），逐页渲染待 B2 后续。
- **第五轮审计修复批次（2026-09-20 晚，报告 `docs/SYSTEM-AUDIT-20260920-ROUND5.md`）**：B2/B3 新代码 P1×3 + P2×8 全部修复——① `fileSmsInboundFilesToMatter` 移入内部模块 `sms/inbound-filing.ts`（不再从 "use server" 导出），加归属校验（转正目标须为该来件匹配案件 + 会话一致性）；② `decideSmsSuggestions` 拒绝混批（ids 须同属一条来件）；③ HEARING/DEADLINE 幂等守卫改事务内重读（同批不再建两个开庭）；④ 视觉 OCR 改走 `aiVision`（visionModel，透传外发台账 logAction）；⑤ guard 加 `allowPrincipal` 选项，确认建议与自动取件内外层口径统一为 handle；⑥ 分析入口防御空 storageKey 与加密部署的已入卷文件；⑦ 取件队列重试改经 `sms/extract-core.ts` 核心复跑（payload 带粘贴人，不再调需要请求上下文的 server action）；⑧ 未匹配来件放开「提取附件」；⑨ 待匹配页签补 suggestions 与阅读状态字段（去掉 as never）；⑩ 手工补传统一 PENDING_REVIEW（不再假「已入卷」）；⑪ 建议全部处理完推进 ORGANIZED。同批修复权限 P2×3（engagements 挂链/终止改 handle 口径并加对象校验、voidIntake 补 assertIntakeEditor、归档交接校验新收尾人资格）与时区 11 个根因点（报表窗口/月桶/编号年份/导出边界/保全对话框客户端组等）。新增测试：confirm-actions 5 例（此前零覆盖）、reports-period 3 例跨时区断言（ymd 改上海口径，防 TZ 自洽掩盖）、engagements 越权 3 例。
- **P3 收尾与复查轮（2026-09-20 晚二批，用户授权「修复剩余 bug 后整查」）**：① 短信 P3 系列——同批建议按 MATTER_MATCH 优先稳定排序（findMany 无 orderBy 使「归属+事项」同批确认顺序不定、事项先执行会整批回滚）、双 MATTER_MATCH 目标不一致拒绝、applySuggestion 归属/幂等守卫合并为事务内单次重读、FIELD_CHANGE 前置值复核（建议值已生效跳过、原值被改拒绝）、开庭时刻格式与 0-23/0-59 范围严格校验（AI 提示同步约束 timeText 形态）、建开庭/期限后即时 refreshScheduleReminderAfterSave、多案件 revalidate 改集合、HTML 页面读取走 readBodyWithLimit、`getOcrSettingsPublic` 补 requireSystemAdmin、HTTP OCR 拒答检测、全失败取件转 NEEDS_MANUAL_FETCH、手动提取后入队分析、ANALYZING/FAILED 可手动重跑、markSmsProcessed note 截断 500、ORGANIZED 仅采纳后推进（全拒可能仍有待处置文件）；② 复查发现并修复——补传到已匹配来件的文件随即转正（原 PENDING_REVIEW 无出口死路）、cron 队列取件对已匹配来件死在 guard 的 requireSession（guard 加显式 actor 参数）、收件人提取已匹配非经办案被 handle 断言误拦（收件人本人放行，非收件人才要求 handle）、归档交接资格补 finance.read 非 ALL 须关联本案分支、审批一致性指纹 plan 键缩为 {serviceCompletedAt,fingerprint} 且按旧算法兼容存量待审申请（主库实测 1 条）、terminateEngagement 去掉挂链 take 20；③ 时区低危收尾 12+ 处（归档目录/README/报表导出/文件名/水印/审计 cutoff 全部 shDayKey 上海口径，schedule 页 ±3 月窗口与 dashboard 12 月窗口按上海月界）；④ 归档审批指纹不再被交接破坏、closure-panel 对 ARCHIVED 案件区分文案、收案向导命中判定改结构化计数（预检发起失败也停草稿）、billing 模式下拉不再提供归档案、批量办结超 100 显式报错；⑤ 权限口径统一——procedures 13 处与元典 bind/unbind、短信 backfill 统一 assertCanHandleMatter（合伙人获全所程序写权，对齐 A 批收敛口径）；custom-fields 保存补 matters.write key（原 CUSTOM 恒拒）；deleteFeeEntry 文案对齐实际校验。未修（如实声明）：建议去重并发唯一约束需 Schema 迁移走审批；核销率>100% 为负向调整的真实数据不掩盖；分诊通道/改期更正/环节级归类为设计项非回归。终态：689 测试全绿（较审计前净增 11 防线用例）、lint/typecheck/build 干净、六页面冒烟正常。

### C/D 批实施记录（2026-09-20，用户授权「继续把全部问题解决」）

- **C 批（恢复能力与单人主线）已实施**：① P1-4 归档收尾死锁修复——收尾一致性指纹只覆盖阻断性清单（程序/事项/保全/交接/待确认款/待审批），实时财务数字独立展示不参与比对；补充归档（supplement）不要求原收尾安排与当前一致，另冻当次快照；归档后收尾责任可交接（`ARCHIVE_CLOSURE_HANDOVER`：仅换收尾人+理由，资格＝matters.transfer 持有者或原收尾人本人，审计+通知）。② P1-7 终止自决——`hasOtherTerminationResolver` 共享判断（申请/决定/页面取数同源），唯一持权人 UI 可达（面板明示「本所无其他有权人」），执行端现算复核不变。③ P2-5 冲突预检命中的收案不再自动送审，停草稿态提示先做结论。④ P2-6 INTAKE 草稿作废终态（VOID 枚举，迁移 20260920000003；先处置紧急事项，审计留痕，不入收案中页签）。⑤ P2-2 尾款下拉保留可收尾归档案（tailWritable）+ 案件入口锁定归属。⑥ 单人主线——「独立执业（律师＋财务）」可审阅组合模板（SOLE_PRACTICE_GRANTS，管理员显式授予）；管理后台角色页持续显示配置缺口（无 finance.confirm 持有者且有待确认款＝红、无 finance.tail 持有者＝黄）；收案自确认从可配清单移除（UI 与服务端一致禁用，配置幻觉消除）。
- **D 批（统计与效率）已实施**：① 本期应收核销率 KPI（本月新增律师费应收中已核销/有效额，同批口径，分母为零不显示）；② 收款分配「按最早到期自动填满」建议（生成→预览→确认，未分配余额保持可见，targets 按到期日排序）；③ 在办案件单案卷宗打包导出（`/api/matters/[id]/export-bundle`：manifest+材料+记录+财务摘要，权限与案件工作簿同口径，审计 MATER_BUNDLE_EXPORT，案件页「导出卷宗」按钮）；④ 低风险任务批量人工办结（`completeTasksBatch`：仅 Task 类型、必填统一处置结果、逐项走单条责任链、单条失败不阻断）；⑤ 对账页从案件入口进入时，登记类操作（登记实收/支出/收费、签署、退回、条件成就）保存成功后自动返回原案件（2026-09-20 第五轮审计修正表述：收款分配、退款与账务更正、分成结算、合同、发票更正的返回动线未实现，随财务批次补齐，不作整体声明）。
- **本轮明确未做（如实声明）**：B2/B3（OCR 云 provider、文书阅读、匹配建议、一次确认应用）——短信专项的阅读与确认层是下一批独立交付；下载流式截断（现有 content-length+buffer 双重检查已限 20MB）；NEEDS_MATCH 私有来件区独立页签（现以状态徽标呈现）；历史统计时点快照（当前核销率为当前口径，历史回溯按 v3 §7 另行核对逐次记录）。

### 第六轮审计修复批次（2026-09-20 晚，报告 `docs/SYSTEM-AUDIT-20260920-ROUND6-v2.md`）

v1 报告两条头牌 P1 经独立核对证伪/收窄后出 v2 修订版，本批按 v2 §九顺序实施 P1 全部 + P2-5 + P3 批（叶森确认时区口径全系统为中国大陆）：

- **P1-1 Docker 部署链**：① compose 的 mailpit 从顶级 `volumes:` 归位 `services:`（此前整个文件非法，`--profile full` 正式部署同样起不来；`docker compose config` 已验证通过）；② 新增 `backups` 命名卷挂 `/app/backups`，README 补「生产应改绑宿主机/异地路径」必读；③ Dockerfile runner 阶段 `apk add bash postgresql16-client`（对齐 postgres:16）+ `COPY scripts`（此前备份脚本根本不在镜像里）+ 预建 backups 目录授权（命名卷首次挂载继承属主）；④ `backup.sh` 变量 `STORAGE_PATH`→`APP_STORAGE_DIR`（与应用/compose 一致，此前自定义存储目录会打包空目录）；⑤ `backup-database.ts` 增加 preflightBackup 自检（脚本存在/bash 可用/pg_dump 可用/目录可写，逐项给出可定位错误）。
- **P1-2+P1-3 保全提醒**：① 保全扫描提取为 `scanPreservationReminders`（`scan-due-reminders.ts`），调度器 2 分钟 worker 接入补扫（09:00 前仅当日关键档，档位/对象/当日去重幂等）——档位命中式触发不再因当日停机永久丢档；② 接收人校验统一走 `isReminderRecipientEnabled`（从 schedule.ts 提出共用）：有效保全负责人→有效案件主办，此前直接投 `ownerId` 不查在职；③ 全部失效时按 `recordOffboardingRisk` 口径升级（持 matters.transfer 资格者，无则 SUPER_ADMIN），记 `PRESERVATION_RECIPIENT_MISSING` 审计，当日去重；④ 过期未续封补团队负责人升级链：escalation.ts 重构出通用内核 `escalateToTeamLeaders`（期限包装行为不变、既有测试全过），新增 `escalateOverduePreservationToTeamLeaders`，未关联案件的孤儿保全跳过案件级访问校验（内容不含案件正文）。
- **P1-4 邮件通道**：① `email-digest` 任务无条件入队（此前未配置 SMTP 连任务都不建）；② worker 跳过/成功/失败均写台账 `reminder-email-last-result`（新模块，SystemSetting 单 K-V）；③ 管理后台「提醒维护」页新增「邮件通道」状态卡（配置状态实时读 env + 最近一次摘要结果 + 未配置琥珀警示与配置指引）；④ 工作台顶部对可进管理后台的用户显示持续缺口警示横幅（律师不可配置，不展示）。
- **P2-5 测试修复**：bugfix-regressions 补 `user.findMany` mock（「待确认实收通知」分支此前全程走 catch、断言在通知从未执行的情况下通过）；「通知发送失败不影响登记」用例改 mock 真实路径的 `createNotification` 并断言 `FEE_ENTRY_NOTIFY_FAILED` 审计（此前 mock 的 `notifyRoleApprovers` 根本不在此路径上）。
- **P3 批**：auth 登录后状态更新改 await + 失败写 `LOGIN_POST_UPDATE_FAILED` 审计（此前 fire-and-forget 空吞，失败计数不清零可致误锁）；dashboard 逾期应收与 finance aging 回退路径改 Decimal 口径（此前 Number() 浮点累加）；`.env.example` 补 `AUDIT_RETENTION_DAYS` 与 `DISABLE_CRON`。
- **新增测试**：`preservation-reminder.test.ts` 4 例（停用回退/全失效升级+当日去重/criticalOnly 档位补扫/EXPIRED+升级链）。终态：693 测试全绿（+4）、lint/typecheck/build 干净、`docker compose config` 通过、本地 dev 站点可达。
- **部署链实测（事后补验，同日）**：实构建 `lawlink-app` 镜像（`docker compose --profile full build` 成功）+ 容器内以 nextjs 用户对运行中 db 真跑 `scripts/backup.sh`——bash/pg_dump 16.15 连库导出 739KB dump、manifest 的 `storage_path` 为 `/app/storage`（`APP_STORAGE_DIR` 变量修复生效）、`/app/backups` 目录可写、产物属主 nextjs；`--profile dev` 的 mailpit 服务启动 healthy（Web UI 200）。本机 1025 端口被系统进程占用，实测经临时端口覆盖完成，不影响 compose 服务定义；实测后 mailpit 已停删，lawlink-db 与 dev 服务未受影响。
- **未做（如实声明）**：02:30 cron 自动备份的持续观察与真实生产部署；P2-1 服务端按规则重算 dueAt、P2-2 冲突名称归一化、P2-3 邮件摘要分页、P2-4 诉讼时效/举证期限预置规则、F-1 送达台账（结构性，含 Schema 走审批）、P3-3 SSRF TOCTOU、F-2~F-6 产品层建议——均按 v2 §九 进 backlog 排期。

### F-1 送达台账批次（2026-09-21，v2 批准执行，阶段一已实施）

- **P2-3 已先行修复（不依赖 Schema）**：email-digest worker 改为先取当日有通知的用户集（distinct userId）再逐人聚合（单人上限 50 条、文末标注），删除全局 take:500 截断；新增 `email-digest.test.ts` 4 例。
- **ReminderDelivery v2**（`docs/REMINDER-DELIVERY-LEDGER-PLAN-20260921.md`，吸收叶森 8 条审查意见）：① objectType×kind 二维拆分（对象与事件分离，唯一键加 kind）；② 状态补 SUPERSEDED（对象变更）/CANCELLED（对象消亡），retireScheduleReminders 与保全处置路径挂作废，投递前复核对象现值（沿用 B3「发送前核实当前状态」）；③ 四个封闭集合字段全部 Prisma enum；④ scheduledAt 改 registeredAt＝登记时刻（即时触发取保存时刻、档位扫描取扫描时刻，修正「恒 09:00」错误）；⑤ 明细保留 180 天（可配）+ 每日清理 job；⑥ 分期改为**保全单点完整闭环 → 扩期限/开庭 → Digest 行+台账卡**（废弃 v1 全量事后补记——只记成功的台账答不了「该发未发」）。
- **迁移已批准执行（2026-09-21）**：独立测试库演练通过后 `prisma migrate deploy` 落主库（20260921000002 已应用并核对 `_prisma_migrations`；演练库 `lawlink_rehearsal_20260921` 保留供复核）。演练同时发现**既有问题**（与本迁移无关）：迁移链从零重放在 20260920000001 报 `FeeEntry.confirmState` 不存在——2026-09-19 业务重建时迁移记录系手工登记，链条不可从零重放；全新安装走 CI 现有空库路径，不重放旧链。此事待单独立项处理。
- **阶段一已实施（保全单点闭环）**：① 登记侧——`scanPreservationReminders` 不再直接创建通知，改经 `registerReminderDelivery`（新模块 `src/server/reminders/delivery.ts`）登记 PENDING 行：OFFSET（接收人锁定）、EXPIRED/ESCALATION/RECIPIENT_MISSING（受众投递时实时解析，userId 空串）；当日同键重复（P2002）计 suppressed，FAILED 未超限重新武装。② 投递侧——`deliverPendingReminders` sweep PENDING 且 registeredAt≤now，逐行**复核对象现值**（B3 原则）：到期日漂移/接收人漂移→SUPERSEDED，解除/删除/过期翻转→CANCELLED，零发送兜住作废-投递竞态；当日通知 findFirst 幂等兜底。③ 作废矩阵——续封 renewProperty→SUPERSEDED；解除 lift/删除 delete/EXPIRED 翻转→CANCELLED；updatePreservationCase 改 remindDays/负责人/关联案件→整案作废。④ 接线——2 分钟 worker「提醒台账投递」（REMINDER_LEDGER_DELIVERY_FAILED_CRON）+ 手动「立即扫描」登记后即时投递。⑤ 扫描计数语义变为「登记数」（audit 加 preservationCountsAreRegistrations 标记），实际送达/作废以台账为准。测试：preservation-reminder 改写为登记断言（4 例）+ reminder-delivery 新增（10 例：送达/档位漂移/接收人漂移/解除删除/EXPIRED 受众回退/升级/无接收人恢复与超管兜底/失败重试与幂等/批量作废/接收人选择），合计 707 测试全绿。



### 第四轮体检与法院短信专项（2026-09-20 确认）

**A 批已实施（2026-09-20）**：12 项代码修复全部落地（权限收敛 `assertCanHandleMatter`、AI 逐件外发资格、程序门禁事务化+零写入回归测试、法人章三条件分立、旧开票入口停用、扣回上限 recoverable、退款免债按核销对应、合同三承接点复核门禁、日期出口归一+存量审计无迁移、责任失效可见最小版、日历搜索发票列表权限）；迁移 20260920000001 触发器修订版经叶森批准已执行（备份在 `backups/hardening-migration-20260920/`，主库 FK RESTRICT/默认 PENDING/AuditLog 触发器拒删三项验证通过，663 测试回归）。存量日期瞬间审计脚本 `scripts/audit-date-instants.ts`：三种瞬间出口归一后均正确，无需迁移。

**B 批设计定案（2026-09-20 叶森确认）**：
- **不做特定法院平台硬编码清单**——开源项目面向多样律所，取件能力采用「通用取件器（直链/HTML 文件链接全量枚举/重定向/错误状态）+ 配置驱动的平台适配器注册表」两层结构：通用层尽力覆盖，适配器按部署方实际遇到的平台逐步贡献，平台覆盖与限制在界面明确展示，未被真实样本覆盖的平台不宣称支持。
- **OCR 采用云 API provider 抽象**（B2 实施）：系统联网部署，管理后台「外部接入」配置云 OCR 服务与密钥，部署方自选厂商；provider 接口统一（图片→文本+置信度+页码），AI/OCR 未配置时保存、取件与人工确认流程照常（降级可见，不显示为分析成功）。扫描件 OCR 是短信首个可交付版本的必需能力，逐页处理、失败页与截断范围可见。
- B1 交付边界：原文即时保存、私有来件暂存（未匹配案件先取件后匹配）、一个链接多文件**全量枚举逐件记录**（不首个成功即返）、后台队列取件、来件处理状态细分（处理中/待人工取件/待匹配/待确认/部分完成/已整理/无需处理）、人工接续（需登录/验证码平台引导律师打开链接完成必要步骤后下载补传接回同一来件，不做浏览器自动化代登录代签收）。补传文件走同一分析流程，不要求重新登记整套材料。粘贴时间、下载时间、文书落款时间、平台签收时间分别保存。

- 第四轮全系统体检报告见 `docs/SYSTEM-AUDIT-20260920.md`（8 项 P1、15 项 P2、产品层问题）；叶森逐项核验后确认修复方向，完整方案见 `docs/SYSTEM-AUDIT-REMEDIATION-V3-20260920.md`。实施顺序四批：A 严重错误与共用基础 → **B 法院短信完整流程（核心交付）** → C 恢复能力与单人主线 → D 统计与效率。B 批验收中「已归档案件来件的补充归档路径」依赖 C 批归档收尾修复，届时补验，不提前宣称完成。
- **法院短信升级为核心办案入口**（2026-09-20 叶森确认）：目标动线为「粘贴一次 → 系统取件、阅读、分类、匹配案件程序环节、形成整理与修正草稿 → 律师集中核对一次确认 → 结果进入案件档案与待办」。设计四条底线：来源原件、AI 提取结果、律师确认结果分别保存可追溯；自动处理充分但正式业务变更仅在有权确认后生效，紧急来件待确认期间仍提醒；案件/程序/环节/材料/事项复用同一套业务对象不另造；失败有状态有出口，「已下载/已阅读/已整理/已办结」不是同一状态。范围限定短信文字及链接文件，不含音频。B 批分三段：B1 可靠收件与取件（私有暂存、多文件、人工登录接续）→ B2 阅读与建议（OCR、文书分类、案件匹配、变更草稿）→ B3 确认与执行（一次确认、原子应用、改期更正、待核提醒、失效接管）。短信专项需要 Schema 设计（来件会话、来源-文件-文书页段、建议与决定），按红线走 create-only 审批。
- **分诊权限通道**（B 批建立，A 批不动短信现状）：律师可处理本人短信与经办案件来件；经配置的管理/行政人员可代为取件、匹配、分类、准备修正，仅获得本次来件与最小匹配信息，不因此开放该案其他材料、财务与任意编辑；正式修改案件信息由该案具备相应办理权限的律师确认；单人执业同一人可录入与确认但两阶段分别留痕；事项责任落当前有效程序主办/案件主办，分诊人只记为处理人。AI 外发逐件校验全文处理资格，管理读取权不自动取得外发资格。
- **A 批实施决定（2026-09-20）**：①结构性案件写入（程序当事人信息、证据新建、材料新版本/归档）收敛到经办断言（主办/成员 + 合伙人岗位例外），管理权（MANAGER_GRANTS）只放大读取；元典回填与短信生成开庭/期限等写路径已于同日补齐收敛（ca1939c），分诊读取入口维持窄口径待 B2/B3 建通道；② AI 审查逐件校验「可读全文 + 可发起审查」，整案批量逐件检查，不以案件可见性代替材料权限；③ `updateProcedure` 事务化，事务内 FOR UPDATE 复核状态与门禁、条件更新、拒绝零写入；④ 日历日统一上海口径，修全部输入与出口（不靠容器 TZ），存量数据未经审计证据不迁移不宣布修正；⑤ 法人章审批与回填分别授权，`allowSelfApproval` 单人例外同时解除本人审批与本人回填限制（仍核对法定代表人身份，审计标注），自确认清单仍硬排除法人章——本人例外不等于事项授权；⑥ 分成扣回上限按 recoverable；⑦ 旧开票入口委托完整校验服务；⑧ 退款免债默认按原核销对应关系，跨应收减免作为独立更正决定；⑨ 补充协议生效（范围绑定/变更激活）补动态冲突复核门禁；⑩ 责任人失效先可见（无合格接收人时通知 SUPER_ADMIN 并记审计，完整接管链在 B 批）；⑪ 迁移 20260920000001 修订为显式拒绝删除 AuditLog 的触发器方案后重新展示审批，风险口径为「漏填默认成已确认，Payment/分成生成仍取决于调用路径」。

---

### 五、开发纪律

1. **改文档优先于改代码**。需求或规范变了，先改 `docs/` 和本文件，再写实现。
2. **新模块顺序**：数据模型 → 类型 → Server Action → UI。不允许先写 UI 再补数据。
3. **业务规则集中**。状态机（`Intake → Matter` 转化、`Matter` 阶段跃迁）、金额计算、权限判定，统一在 `src/lib/` 或 `src/server/`，不要在 React 组件里写。
4. **不为了让代码跑起来**注释掉校验、绕过权限判断、`@ts-ignore` 整段。找根因。
5. **密钥、token、密码**不进代码、不进 commit、不进日志、不进截图。`.env` 永远在 `.gitignore`。
6. **Prisma 迁移**先用 `prisma migrate dev --create-only` 生成 SQL 给叶森看，再执行。生产迁移走 `prisma migrate deploy`。
7. **删除文件、`.env` / CI 修改、`git reset/rebase/push`、生产部署**必须先征得叶森确认（红线）。
8. **附件**默认私有存储在 `storage/`，下载经鉴权 API，不暴露公开直链。

---

### 六、权限与审计

#### 业务端与管理后台分离（2026-09-06 确认）

- LawLink 保持单体应用、单库和统一登录；新增 `/admin` 独立管理后台页面壳，不另建部署或日常管理员账号。本人账号可同时承担业务岗位和系统管理职责，审计始终落在同一 `User.id`。
- 业务端“个人设置”只保留本人资料与登录安全；律所信息、用户、岗位角色、律师团队、审批权限、归档制度、模板、自定义字段、外部接入和审计日志统一从管理后台进入。管理后台持续显示当前处于系统管理模式，并提供返回业务系统入口。
- 第一阶段曾只分离路由、导航与页面壳；第二阶段已按 `docs/ADMIN-CONSOLE-PLAN.md` 以全新版本标准拆分业务岗位、系统管理资格和审批资格，不保留旧数据、旧角色或旧网址兼容逻辑。
- `/admin/*` 为后台唯一入口，`/settings` 只保留个人资料与登录安全。系统管理操作统一校验 `SUPER_ADMIN`；主任律师原有提醒维护和批量导入资格保留。系统身份变更须失效旧会话、记录审计并保护最后一名有效超级管理员。`UserRole` 不包含混合型 `ADMIN`，超级管理员另持真实业务岗位。Schema 变更和旧路由删除已批准；新迁移 SQL 在执行前仍须单独获得明确批准。
- 新管理页面入口位于 `src/app/(admin)/admin/`，管理页面壳位于 `src/components/layout/admin-shell.tsx`；权限判断仍在服务端既有位置执行，不能只靠隐藏导航限制访问。

#### 自定义岗位角色（2026-09-06 批准）

- 按 `docs/ROLE-MANAGEMENT-PLAN.md` 实施角色管理及用户分配。保留五个内置角色，新增 `CUSTOM` 与角色定义、权限配置；每人一个岗位角色，审批权限组仍独立配置。
- 仅超级管理员管理角色；“行政”按既有固定角色记录标识纳入内置角色。全部内置角色允许编辑显示名称和介绍，不能修改权限、停用或删除；名称不参与授权判断。五个原有角色的显示配置复用 SystemSetting，“行政”沿用现有 RoleDefinition，不改 Schema、不迁移账号。自定义角色支持编辑和停用但不物理删除。停用自定义角色暂停所属账号业务访问及当前审批资格。
- 内置角色名称与自定义角色名称统一校验重名；显示资料修改检查并发版本、事务内复核管理员并记录审计。改名不撤销会话或改变审批、财务、团队等授权；界面和人员列表使用当前显示名称。
- 自定义角色按固定功能权限和数据范围授权，未知配置默认拒绝；读、写、财务、附件及导出分别校验。团队只读不能授权写入、下载或导出，申请级审批不能授权案件全文。
- `src/lib/roles/` 存权限目录、解析和范围规则，`src/server/roles/` 存角色校验及事务管理，`src/app/(admin)/admin/roles/_components/` 存角色页面组件；文件用英文短横线，测试在 `src/tests/` 对应目录。
- 本次方案和 Schema 修改、生成迁移 SQL 已获批准；执行数据库迁移仍需展示 SQL 后另行确认。create-only 不支持时允许仅对本次前后 Schema 使用 migrate diff 生成 SQL，禁止重置数据库或混入其他迁移。

#### P0 底线补强批次决策（2026-09-13 批准，叶森授权按方案直接执行）

- 依据 `docs/P0-IMPLEMENTATION-PLAN.md` 与 `docs/TARGET-MODEL-PLAN.md` 实施改进报告 v5 的 P0 八项。本批 Schema 变更与迁移 SQL（20260913052823_p0_baseline_batch）已获批准并执行；后续各批迁移继续走 create-only/diff 生成、展示 SQL 后执行的流程。
- **客户主数据（P0-1）**：身份持续唯一策略——证件类型 + 规范化号码部分唯一索引（仅约束已填号码且未删除的行），存量行补录证件类型后纳入约束。建档、收案、批量导入三入口统一查重；匹配复用与存量合并分别验收；合并只调整当前主体关联，不改写历史身份快照，保留映射与合并依据。
- **登录锁定（P0-3）**：连续 5 次失败锁 15 分钟，成功登录清零，锁定拒绝写审计。
- **期限来源（P0-8）**：规则生成的期限以待确认状态落库；已确认/已调整期限不被规则重算静默覆盖；法定期限规则管理界面位于管理后台"提醒维护"页，内置规则不可删除。
- **制度决策 4.1（团队历史访问，选项 A）**：维持团队汇总默认，新增案件级"受限事项"标记（`Matter.teamAccessRestricted`）——受限事项不进入团队汇总视图，限制优先于团队授权；个人直接授权不受影响。
- **制度决策 4.2（证件采集）**：维持现行默认（实名账号登记证件并留存照片）；账号实名、身份核验、证件照片留存按目的分别评估的原则记入本文档，未来如引入受限协作账号按该原则另行设计。
- **制度决策 4.3（低影响自确认，2026-09-13 已实施）**：管理后台「审批权限」页提供自确认清单（文书送审/用章申请[非法人章]/收案审批三项可配），命中清单的提交由申请人自我确认即完成原审批环节，审计动作标记 `*_SELF_CONFIRM`。归档、开票、盖章回填与法定代表人章（含 requiresLegalRep 章种）由服务端硬排除，任何配置不可自确认；附带法人章的用章申请整单不走自确认。收案审批清单可配，转化接线随收案专项启用。实现位于 `src/lib/approvals/self-confirm.ts` 与 `src/server/approval-permissions/self-confirm-actions.ts`。

#### 个人身份与基本资料（2026-09-06 确认）

- 本人可维护姓名、手机号和登录邮箱；邮箱变更校验当前密码、撤销旧会话。角色和权限不在个人资料中修改。
- 人员最终身份标识采用“证件类型 + 规范化证件号码”，内部业务关联始终使用 `User.id`。支持居民身份证、港澳台居民居住证、往来港澳通行证、港澳居民来往内地通行证、台湾居民来往大陆通行证、护照、外国人永久居留身份证和自定义其他证件；`OTHER` 必须填写证件名称。停用账号仍占用复合唯一标识。
- 管理员新增用户时必须选择证件类型并登记号码；**证件照片为选填**（2026-09-13 修订：账号开通与证件照片采集解耦——照片可在开通后经「资料 → 身份证件」补充上传，涉及身份核验的场景按需在核验前采集）。视觉识别只作预填，管理员必须对照原件确认。非本地 AI 服务每次识别前展示服务域名并取得当次确认，识别失败允许手工录入，不把 OCR 表述为实名认证或真伪鉴定。
- 证件照片独立于案件 `Document`，始终用 AES-256-GCM 加密写入私有 `storage/user-identities/{userId}`，只能由本人或 `SUPER_ADMIN` 经鉴权接口查看。旧照片更正时标记被替代并保留审计，不物理删除；不得进入案件权限、通用导出、全局搜索或公开直链。
- 证件号码、手机号在有权限的页面内直接明文展示，不做打码与「明文/隐藏」切换（2026-09-18 用户确认：律师办案需随时核对号码，反复展开浪费操作）；访问控制仍靠权限而非遮蔽——证件号码与图片只允许本人或管理员查看。自动识别、号码查看、图片查看、上传替换和管理员更正均审计，日志不得记录完整号码、图片、原始文件名或 AI 原始响应。
- 历史账号未登记号码时保持可用，不推测或回填。资料修改与审计同事务提交，并校验当前权限、账号状态及并发版本。
- 实现沿用 `src/server/users/`、个人信息页与用户管理页；证件规则集中在 `src/lib/identity-documents/`，识别、上传和访问控制集中在 `src/server/identity-documents/`，跨页表单放在 `src/components/users/`。设计说明见 `docs/PERSONAL-PROFILE-PLAN.md` 与 `docs/IDENTITY-DOCUMENT-PLAN.md`。本次 Schema 修改已批准；迁移执行必须在正式 SQL 展示后获得确认，已执行迁移不得重复运行。若 `migrate dev --create-only` 因非交互环境无法运行，允许用 `prisma migrate diff` 对本次修改前后 Schema 生成 SQL；该替代仅生成文件，不得重置数据库或执行迁移。

#### 按事项审批（2026-09-06 确认）

- 岗位角色与审批授权分开；管理员管理账号、审批权限组与用章事项。审批规则以操作、案件类别、用章事项、印章类型组合，组间取并集、规则内取交集；空选择不代表全部。
- 审批授权仅覆盖对应申请及必要附件，不扩大案件成员、团队、财务、导出权限；团队负责人不自动获得审批权。
- 收案、文书、归档、开票、用章审批及盖章回填分别授权。所有人员（含超级管理员）审批法定代表人章时核对当前法定代表人；默认不得审批本人申请，单人执业例外由管理员显式开启并审计。
- 授权、停用与账号状态实时检查。权限变更和审批结果与审计同事务提交，已处理申请不得重复处理。
- 所有账号（包括 `SUPER_ADMIN`）统一按事项授权，系统管理身份不提供审批或盖章回填捷径。无合格审批人阻止提交；新系统不读取旧角色审批授权，也不提供历史未分类申请的补分类入口。审批人仍须遵守账号有效性、事项及印章启用状态、事项与印章适配、本人审批开关、法定代表人章身份限制、申请状态和审计要求。
- 实现目录：`src/lib/approvals/` 放集中规则与鉴权，`src/server/approval-permissions/` 放管理操作，`src/app/(admin)/admin/approval-permissions/` 放管理界面，`src/app/(app)/approvals/` 放统一审批工作台；测试沿用 `src/tests/`。设计与迁移说明存 `docs/`，迁移 SQL 存 `prisma/migrations/`。生成 SQL 不等于授权执行迁移。Playwright 验收输出放 `output/playwright/`；CLI 首次运行产生的 `.playwright-cli/` 仅为本地临时快照，不入交付，清理前遵守删除确认规则。

#### 统一审批工作台（2026-09-06 确认）

- 后续使用以全新系统的完整流程为准：收案冲突检索自动携带申请中各主体的身份，关联收案的检索不得省略身份；审批主界面直接展示本案核查对象及身份、具体命中和人工意见，不以旧数据兼容解释组织页面。查询条件及既往检索仍可展开复核；资料不完整时直接提示重新检索，不推测身份、不清空记录。

- 立案审批视觉方案已确认：宽幅审阅窗口、顶部案件摘要、申请资料/冲突核查/附件/处理记录页签、桌面右侧固定审批区及窄屏底部操作。完整字段仍可查看，未填写的非关键项可折叠，风险在首屏提示；切换页签保留意见，沿用全部授权及校验。呈现组件及局部 CSS 放在 `src/app/(app)/approvals/`，不改变其他审批类型；细则见 `docs/UI-APPROVAL-DETAIL-PLAN.md`。
- 叶森已确认将同类视觉优化扩展到其他审批与弹窗：审批采用统一宽幅审阅框架，普通表单与确认框按内容保持适当宽度。公共标题、表面、操作区及响应式规则在原子层统一，审阅布局与分区组件放 `src/components/patterns/`；分批迁移实际审批和常用表单，保留入口、字段、校验、授权和审计。实施清单见 `docs/UI-DIALOG-CONSISTENCY-PLAN.md`。

- 立案审批详情按原填写项目完整展示已保存的申请信息（含程序、当事人、标的、服务范围、收费、承办人员及附件），敏感信息按申请查阅权限直接明文展示，不做打码与展开切换。冲突审查展示各次检索对象、命中字段及理由、风险提示、人工结论及说明；未命中不表述为人工确认无冲突。关联案件仅展示冲突核查所需的编号、名称、主办及命中角色，不因审批授权开放其他案件正文。关联档案当前值与历史检索记录须明确区分；缺失快照不得补造。
- 用章申请里「用章事项」与「本次用印说明」职责分明，不得再出现同义两选（2026-09-19 用户确认）：**事项**＝管理后台配置的分类，决定审批路由与可用印章；**说明**＝申请人一句话写清这次为什么盖章（自由文本，限 60 字）。原先的固定事由预设（委托合同 / 法律意见书 / 所函 / 证明 / 其他）与事项分类语义重叠，已删除。
- `/approvals` 统一收案、文书、归档、开票、用章的待办、历史、详情及执行环节；提供待我审批、我已处理、我的申请、全部记录（仅 `SUPER_ADMIN` 审计查看）四个视图。已批准待开票/待盖章单独标明执行环节，不能再次驳回已批准事项。
- 申请人、实际处理人可查看该申请及处理历史；收案主办保留原有补正和重新提交权限；当前审批授权仅提供当前待处理环节的访问；超级管理员可审计全部申请，并按上述超级管理员规则处理当前有效环节。以上不扩大整个案件的访问、写入及导出权限。
- 历史优先展示逐次审计事件，业务字段仅补充有明确人员和时间的旧记录；缺失信息标明未记录，不推断。详情明确区分当前申请内容和历史处理记录。新处理意见写入同事务审计，重复送审不得抹掉前次意见。
- 申请附件逐件校验；实际处理人可读取该申请关联的原附件，重新提交后新加的附件不按前次处理关系开放；历史查阅不开放案件的其他附件。统一工作台文件沿用 `src/lib/approvals/`、`src/server/approval-permissions/`、`src/app/(app)/approvals/`，不新增数据库表或迁移。

#### 归档材料审阅与完整性（2026-09-06 确认）

- 归档清单必须关联实际案件材料。申请人勾选、文件存在和审批人确认是三个不同事实；无文件的必交项不得显示为齐全。必交缺项或不适用须逐项说明，审批人明确核准例外后才能通过。
- 管理员在“设置—归档制度”确认本所采用的制度名称、版本、生效日期及制度原文。申请固定送审时的制度与清单版本；未配置本所制度时不得提交正式归档申请，不把系统内置参考清单表述为本所制度。
- 归档申请固定当次文件名称、大小、内容校验值、清单关联和顺序。审批详情逐项展示实际材料、缺项说明、人工核验事项及制度依据；归档审批授权只开放该申请固定的材料，不扩大案件正文、财务、其他附件或整案导出权限。
- 提交和审批通过时均由服务端核对文件归属、有效性和内容校验值；待审文件不得删除。旧申请不能还原材料范围时标记为历史未固定并退回补正，不按当前案件材料推断过去齐全。
- 驳回后重新提交形成新申请并保留旧记录。批准后的归档包严格按该条批准记录固定的材料生成；文件缺失、读取失败或内容变化时停止导出，导出路径和校验值只写回目标归档记录。
- 实现复用 `ArchiveRecord.checklistJson` 保存有版本的材料快照、`SystemSetting` 保存归档制度配置、`AuditLog` 保存逐次处理与审批核验。本期不改数据库 Schema；历史布尔清单只兼容读取，不升级为已核验快照。

#### 律师团队（2026-09-06 确认）

- 常设律师团队与案件承办人员分开。仅系统超级管理员（`SUPER_ADMIN`）创建/停用团队、指定负责人、设置成员和额外查看人员；团队负责人不因此获得全所管理角色。
- 负责人及获授 `canViewAllMatters` 的成员，自动查看当前团队成员原始立案或当前主办的全部案件（含历史案件）；不要求逐案关联团队。协办关系不向协办人员的整个团队传播权限。
- 按当前有效团队、有效成员关系实时判定；退出团队、撤权或停用团队即停止新增的团队访问，个人已有经办权限保留。账号禁用不使其历史案件从团队汇总消失，但禁用账号不得继续访问。
- **保留团队历史访问自动汇总**（2026-09-13 复议维持）：一至五人小所人员调组频率低、历史回看高频，自动汇总的实际价值大于扩散风险；越权面由案件级受限标记（`teamAccessRestricted`）收窄。**撤权复核提示**：管理后台保存移出成员、更换负责人或停用团队时，界面须提示"该成员经团队汇总可见的历史案件访问即时停止，如有仍需其访问的受限案件请另行复核"；提示不阻塞保存。
- 原始立案人：收案转化取原收案创建人，直接立案取创建人；历史资料只按收案记录/创建审计回填，无法确定时留空。批量导入保留操作人记录，当前主办仍纳入汇总。
- 团队新增授权是只读案件正文、进度、期限及记录的授权；财务、分成、附件下载、导出和业务写入沿用个人经办/既有全所角色权限。禁止使用只读访问断言授权写入。
- 实现约定：`src/server/teams/` 放团队校验和服务操作，`src/lib/teams/` 放共享选择规则，`src/app/(admin)/admin/teams/_components/` 放团队设置页组件。沿用英文短横线文件名；测试在 `src/tests/` 对应目录。迁移及审阅说明分别存 `prisma/migrations/`、`docs/`；不另建临时产物目录。

#### 制度级决策（2026-09-13 确认，改进报告 v5 第八章）

- 决策程序：v5 报告第八章将「团队历史访问默认」「建号必传证件照片」「普遍按事项审批」三项列为例行复审项（第三项已于 2026-09-06 落地为按事项自确认配置，不再重复决策）。叶森于 2026-09-13 将余下两项的决定权委托给实施方，本节即修订记录，先改本文件、再改代码。
- **团队历史访问默认：保留自动汇总**。比较要点：本产品定位一至五人小所，调组频率低而历史回看高频；越权阅读后果已由案件级受限标记收窄，撤权即时生效已有实现。配套补齐撤权复核提示（见律师团队一节）。若未来部署到十人以上团队，应重新评估改为显式授权。
- **建号必传证件照片：解耦**。账号开通仍强制证件类型与规范化号码（最终身份标识），照片改为选填、可在开通后补充；是否采集照片按身份核验目的单独判断，不由账号开通强制。理由：账号实名、身份核验与照片留存是三件事，强制耦合使新成员入职即被挡，而照片的留存价值依赖核验场景。

- **业务岗位**（V1）：`PRINCIPAL_LAWYER` / `INDEPENDENT_LAWYER` / `LAWYER` / `ASSISTANT` / `FINANCE` / `CUSTOM`；系统管理身份另用 `SystemRole.SUPER_ADMIN`，不得从业务岗位推导。
- **岗位分类口径（2026-09-19 用户确认）**：内置律师岗位按行业惯例展示——合伙人（`PRINCIPAL_LAWYER`，原「主办律师」，全所查看、审批路由等既有权限不变）、独立律师（`INDEPENDENT_LAWYER`，本批新增枚举值，权限与授薪律师完全一致）、授薪律师（`LAWYER`，原「经办律师」）、律师助理（`ASSISTANT`，原「助理」）。仅新增枚举值与改显示名，不改既有枚举值、不迁移存量账号（管理员按人员实际身份在后台逐个调整）。历史文档与决策记录中的「主任律师／主办律师」即现「合伙人」。
- **业务管理权与岗位解耦（2026-09-19 用户确认，`User.managerAuthorized`）**：岗位只表达职业分类，业务管理权按人授予——获授权者（无论何种岗位，含独立律师、授薪律师）在业务端等同合伙人：全所案件/收案/客户/财务/归档可见、报表、导入、归档导出、公告与制度维护、联系人审核、用章他人取消、后台提醒维护资格。实现：`isManager` 收用户对象（字符串入参仅认合伙人岗位，供纯岗位查询用）；可见性过滤层经 `resolveRoleUser` 合成的 `MANAGER_GRANTS`（matters/clients/finance/archive.read ALL）识别。授予/撤销仅管理后台用户页（`setUserManagerAuthorized`），与审计同事务、并发校验；每次会话实时解析，即时生效、不撤销在线会话；超级管理员可授予本人。系统管理身份（`SUPER_ADMIN`）仍不自动获得业务权限——须显式授此权。写入、案件处理、审批、导出等敏感操作仍走各自独立授权，不因管理权自动放大（`assertCanAssociateMatter` 等经办断言不变）。
- **可见性**：`Matter` 保留原有个人/全所角色权限，并按上文律师团队规则增加团队只读权限；`FINANCE` 可见所有案件的财务字段但不能编辑案件正文。
- **跨案件最小披露例外——冲突检索**：冲突检索结果会跨越案件可见性，向检索人展示命中案件的**系统编号、案件名称、主办律师姓名、当事人角色**（另附案件状态与案由类别，供判断是否同一事项；不含正文）。对**非该案成员**不给跳转链接、不返回 `matterId`。若检索人本就是命中案件的主办或成员（既有案件关系，非因检索获得），返回 `matterId` 与「查看案件」链接——成员打开自己案件不构成越权。理由是不跨案件就无法尽职审查，且检索人需要知道找谁核实。**例外仅限于此**：案件正文、财务、材料一律不得因冲突检索而对非成员开放。
- **审计日志**（`AuditLog`）必须记录：登录/登出、案件创建/查看/编辑/归档、材料下载、财务变更、冲突检索、权限变更。
- 第一版**不做字段级权限矩阵**，能用"角色 + 案件成员"覆盖的就不上更复杂的方案。

---

### 七、验证命令（每次改完主动跑）

```bash
npm run lint              # ESLint CLI
npm run typecheck         # tsc --noEmit
npm run prisma:validate   # Prisma schema 校验
npm run build             # 生产构建（最严的检查）
```

UI 改动还要：在浏览器里把"金线"（典型工作流）走一遍，不只看类型/构建过。

每次修改后还必须确认本地站点仍可访问：先核对端口监听进程属于 `/Users/yesen/Code/LawLink`，再打开 `http://localhost:3000/login` 或本次涉及页面。若服务已停止或端口不属于本仓库，先恢复/切换到正确 LawLink 服务并重新验证后再交付；不能只报告代码验证通过。

---

### 八、安全底线

- 附件私有，下载链接带短期 token。
- 案件正文、客户联系方式、身份证件号码、身份证件照片、统一社会信用代码视为敏感数据：控制在「谁能进这个页面」，进入后直接明文展示，前端不再提供“打码/明文”切换（2026-09-18 用户确认）。证件图片仍按需加载并鉴权。
- 日志中不输出 PII（身份证、电话、住址完整值）。
- `AuditLog` 不可由业务代码删除，只能归档。

---

### 九、文档清单

| 文件 | 作用 |
|---|---|
| `AGENTS.md`（本文件） | 工作区铁律，所有协作者必读 |
| `docs/PRD.md` | 产品需求与功能范围 |
| `docs/DATA-MODEL.md` | 数据模型详细设计与 ER 图 |
| `docs/UI-DESIGN.md` | 设计语言、色板、关键页面 wireframe |
| `README.md` | 仓库门面，安装运行说明 |
| `CHANGELOG.md` | 版本变更（V1 发布时建立） |

---

### 十、给 AI 助手的特别说明

- 默认中文回应，代码/字段/路由用英文。
- 结论先行，方案有问题直接指出。
- 涉及红线（删文件、改 `.env`/CI、`git push`/`reset`/`rebase`、生产部署、Schema 迁移）**必须**先征得叶森同意。
- 不为了"看起来很忙"提交大量改动。每次改动控制在可审阅的范围。
- 自动验证清单见第七节。


### v1.3.0 版本归集（2026-09-13）
- 按用户授权归集北京时间 2026-09-10 之前的未提交开发，以 2026-09-07 历史文件快照为基准。
- 本版本包含后台分离、岗位/团队/审批权限、统一审批、归档审阅及身份资料改造；不包含 9 月 13 日的 P0/P1 和 v4 界面开发。
- 仅发布代码与版本标签，不执行数据库迁移或生产部署；运行数据、合同工作产物及缓存不纳入版本。
- 发布验证发现既有 instrumentation 使用 webpackIgnore 导致生产启动缺失调度模块；本版附带最小打包修复，保留运行时、开发环境和 DISABLE_CRON 判断。


### v1.3.1 公开说明与 CI 维护（2026-09-13 批准）
- 用户已批准修复 GitHub CI，并要求审校公开说明。使用独立发布副本，不纳入下一版在途功能。
- 模型漂移检查必须使用独立 shadow 数据库，全新安装测试保留自己的空库；不得通过跳过校验、基线标记或清空业务数据规避失败。
- README、安装/升级说明和安全说明按已发布功能核对；旧设计与体检报告标注历史范围，不把未来计划或旧安全审计结果作为当前保证。
- 维护版 v1.3.1 保留 v1.3.0 标签；本次不操作本地业务数据库或生产部署。


### v1.3.2 依赖安全维护与空白安装（2026-09-13 批准）
- 用户要求修复依赖问题，并明确现有案件数据为模拟测试用途；公开系统不携带案例业务数据。
- 在独立发布副本升级现有依赖的兼容修复版本，验证后发布维护版；保留下一版在途功能。
- 默认初始化只包含管理员和系统基础字典、模板、规则；不得创建案件、客户或财务演示记录。Document 表仅允许系统内置空白模板文件，业务材料必须为空；测试断言验证空白安装；本次不清空本地数据库、不删除用户测试数据或重写 Git 历史。
- 依赖审计按生产与全量分别记录，零已知告警只代表当日审计结果，不等同整体安全保证。现有 CI 增加全量审计与空白业务表断言，沿用已获批准的 CI 维护范围。

### 审计节奏与并发协作（2026-09-20 确立）

四轮体检（09-19/09-20，累计 P1×22、P2×45+ 修复）+ 第五轮（09-20 晚，回归验证+已知清单核对口径，报告 `docs/SYSTEM-AUDIT-20260920-ROUND5.md`：678 测试全绿但短信确认层 3 P1 + 8 P2 零测试覆盖、时区第二形态 33 处误用、C/D 批 10/12 落地正确）后评估：不存在「修复引入回归」死循环，但确认三个真实风险——实例修复打地鼠、大修新代码自身产 bug、审计无止损线。对策如下：

- **审计止损线**：下一轮审计限定为「回归验证 + 已知清单核对」，不引入新审查镜头；新发现一律进本节 backlog 排期修，不当场修。发现与修复分离是打断审计跑步机的核心手段。
- **类修复优先于点修复**：同类问题（权限断言、时区取日、状态门禁）出现第二例时，全仓检索同形态调用点一次修完；提交信息须记录核对范围与保留原因（例：f4bd8b1 记录 47 处核对、11 处修复、其余判定安全的原因），不允许只修被点名处。
- **冻结窗口**：B2/B3 短信阅读确认层收尾前不启动新功能批次，避免新代码持续喂养审计循环。
- **单写者原则**：本仓库同一时间只允许一个 AI 代理执行写操作。2026-09-20 实测发现 Codex 与 ZCode 并发改同一工作区（B2 实施与体检收尾互相覆盖、测试结果互相污染、一方提交混入另一方半成品）。启动任何代理前先确认无其他 codex/claude/zcode 进程在写本仓库；发现并发立即停止写操作并与叶森协调保留哪一个。
- **backlog**（排期处理，不与当日修复混做）：
  1. ~~迁移 20260920000001 待批执行~~ 已过时作废：第五轮实测主库 `audit_log_no_delete` 触发器、`FeeEntry.confirmState` 默认 PENDING、迁移 000001–000004 均已应用（A 批记录属实，当时 backlog 未同步）；
  2. 元典回填与短信分诊的 B 批通道细化（写断言已收敛 `assertCanHandleMatter`，见 ca1939c；第五轮修复已把 extract/match/转正改齐 handle 口径，分诊授权面与 UI 通道仍未实现）；
  3. ~~时区第二轮清扫~~ 已完成（2026-09-20 第五轮修复批次落地：11 个根因点全修——reports periodPresets/customPeriod、dashboard/finance 回退路径月桶、seals 月窗与流水号年份、四类编号生成器年份、insights 年份、matters 导出日期边界、preservation-dialog 客户端组、schedule query 默认起点、imports ExcelJS 取日、backfill 脚本；reports-period 测试断言改上海口径防 TZ 自洽掩盖。剩余低危：12 处 toISOString 文件名/目录类 UTC 取日、schedule 页 ±3 月窗口与三个模糊窗口、alerts/保全 setDate 类）；
  4. B2/B3 短信阅读确认层收尾（进行中）：第五轮审计 P1×3 与 P2×8 已于同日修复批次全部落地；P3 系列同日第二轮修复绝大部分完成（同批建议按 MATTER_MATCH 优先稳定排序、双匹配目标不一致拒绝、FIELD_CHANGE 前置值复核带幂等出路、开庭时刻格式与范围严格校验、AI 提示约束 timeText 形态、建开庭/期限后即时刷新提醒、多案件 revalidate、HTML 页面流式上限、OCR 设置读取补鉴权、HTTP OCR 拒答检测、全失败取件转待人工、手动提取后入队分析、ANALYZING 卡死可重跑、note 截断、补传即转正、收件人提取动线恢复、ORGANIZED 仅采纳后推进、归档指纹兼容存量申请）。剩余：建议去重的并发唯一约束（需 Schema 迁移，走 create-only 审批）、缴费类 Task 通道；深化项不变（改期更正、环节级归类、来源页回看）；
  5. ~~权限 P2×3~~ 已完成（2026-09-20 第五轮修复批次：engagements 三入口改 handle 口径/终止加对象校验、voidIntake 补 assertIntakeEditor、归档交接校验新收尾人资格）；
  6. ~~文档修正 D12~~ 已完成（表述收窄为登记类操作，见 D 批记录修订）；
  7. M-1/M-2 单人执业引导、独立执业角色模板与配置缺口警示（第四轮产品层建议）。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
