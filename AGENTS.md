## LawLink 工作区规则

> 这是项目的"约束先行"文档。任何 AI 助手（含 Claude Code）或人类协作者在本仓库工作前，必须先读完本文件。
> 规则与实践冲突时，先改本文件、再改代码。

---

### 一、项目定位

- **LawLink** 是面向独立律师、小团队和小型律所的**开源、自部署**案件/项目管理系统。
- 第一版主线工作流：
  `收案登记 → 冲突检索 → 转正式案件 → 持续跟进 → 财务记录 → 结案归档 → 数据导出`
- **不做共享 SaaS 多租户**。一个律所/团队部署一套实例，单体应用 + 单库。
- 协议：**MIT**。

---

### 二、技术栈（已定型）

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | Next.js 16 App Router + TypeScript | SSR + RSC，单仓单进程 |
| UI 组件 | **shadcn/ui** + Radix UI | 拷贝式组件，深度可定制 |
| 样式 | **Tailwind CSS** | 高密度浅色工作台，保留主题扩展能力 |
| 动效 | **Framer Motion** | 微交互、页面切换 |
| React | React 19 | Next.js 16 配套 |
| 图标 | lucide-react | |
| 图表 | Recharts | 工作台、财务统计 |
| 表单 | React Hook Form + Zod | |
| 表格 | TanStack Table | 表头筛选、排序、分页 |
| 数据库 | PostgreSQL 16 + Prisma 5 | |
| 鉴权 | NextAuth.js（Credentials Provider） | 邮箱密码登录 |
| 部署 | Docker Compose（app + postgres） | |

**已弃用**：Ant Design。不要再引入 `antd` 或 `@ant-design/*`。

**预留接入**（V1.5）：元典开放平台 MCP（law/case/company），用于冲突检索增强和企业核查。

---

### 三、目录约定

```
LawLink/
├── docs/                  # 设计文档（PRD / DATA-MODEL / UI-DESIGN）
├── prisma/                # schema、迁移、seed
├── public/                # 静态资源
├── storage/               # 本地私有文件（不入 git）
├── src/
│   ├── app/               # 路由与 Route Handler
│   │   ├── (auth)/        # 登录、注册路由分组
│   │   ├── (app)/         # 登录后的主应用路由分组
│   │   ├── (admin)/admin/ # 独立管理后台页面壳；仍复用同一登录、服务层与数据库
│   │   └── api/           # API 路由
│   ├── components/
│   │   ├── ui/            # shadcn/Radix 原子组件与全局无障碍、动效适配
│   │   ├── patterns/      # LawLink 跨页面视觉与交互模式
│   │   ├── layout/        # AppShell、Sidebar、Topbar
│   │   ├── matters/       # 案件相关组合组件
│   │   └── ...
│   ├── lib/               # 业务规则、工具函数、Prisma client
│   │   ├── auth/          # NextAuth 配置、session helpers
│   │   ├── permissions/   # 角色权限、案件可见性
│   │   └── ...
│   ├── server/            # Server Actions、服务层
│   ├── types/             # 共享 TS 类型
│   └── styles/            # globals.css、Tailwind config 扩展
└── tests/                 # （V1.5 引入）
```

**规则**：
- `src/components/ui/` 只放 shadcn/Radix 原子组件。允许在原子层维护全站统一的语义 token、无障碍和基础动效适配，但禁止加入 LawLink 业务结构或领域判断；更新 shadcn 时必须审计并保留这些适配。
- `src/components/patterns/` 放 `PageHeader`、`ListToolbar`、`FormSection`、全局动效策略等 LawLink 品牌化跨页面模式，不承载 Server Action 或领域状态机。
- 跨页面复用的业务组件提到对应领域目录，仅单页用的组件就近放在路由目录下 `_components/`。
- 业务规则（金额计算、状态机、冲突匹配算法）一律沉淀到 `src/lib/` 或 `src/server/`，禁止散落在页面里。

---

### 四、命名约定

| 对象 | 风格 | 例 |
|---|---|---|
| 路由目录 | 小写英文复数 | `matters/`、`intakes/`、`conflicts/` |
| React 组件 | PascalCase | `MatterCard.tsx` |
| 工具函数 | camelCase | `formatCurrency`、`computeReceivable` |
| Server Action | camelCase + 动词开头 | `createMatter`、`closeMatter` |
| Prisma model | PascalCase 单数 | `Matter`、`Client` |
| Prisma 字段 | camelCase | `caseNumber`、`createdAt` |
| 数据库表（自动） | snake_case | Prisma `@@map` 控制 |

**术语固定**：
- 收案线索 → `Intake`
- 正式案件/项目 → `Matter`
- 客户（机构/个人） → `Client`
- 客户联系人 → `Contact`
- 当事人/相对方/第三人 → `Party`
- 收/付费记录 → `FeeEntry`
- 结算单（合同/账单） → `Billing`
- 法定期限 → `Deadline`（带 `category`：诉讼时效/举证/上诉/履行/其他）

**界面语言**：面向用户的按钮、状态、指引、说明和无障碍提示默认中文；数据库/API 枚举须转换为中文标签后展示。品牌名、文件格式、必要的技术配置标识和用户原文可保留英文。文件上传控件须提供中文选择提示，不依赖浏览器系统语言。展示映射集中在 `src/lib/`，不得改变已保存枚举值。

**v4 墨案 UI 重建（2026-09-13 批准）**：界面以 `docs/mockup/v4/` 效果图为验收基准全面重建，不在旧页面上换色小修；方案与页面对照见 `docs/UI-V4-REBUILD-PLAN.md`。原子层 `src/components/ui/` 直接使用墨案 token（`src/app/moan.css`），不得再写 v3 硬编码色值；页面骨架与签名母版（PageHeader、Panel、SpineRow、RiskLadder、ProcedureChain、ReviewSeal 等）统一放 `src/components/patterns/`。重建不改 Schema、权限与 Server Action 语义，效果图未画到的旧模块须归位不得删除，不做无后端支撑的假功能。

**界面交互原则（2026-09-14 审查确立，详见 `docs/UI-REVIEW-20260914.md`）**：① 一屏一个主操作，由页面通过 `useTopbarAction` / `TopbarLinkAction` 注册到顶栏，页头与工具栏不重复同一主按钮；② 单选项 ≤5 用 chip，>5 用下拉（`ChoiceField`，`RadioChips` 超 6 项自动收为下拉）；③ 禁止原生 `confirm / prompt / alert`，确认用 `confirmDialog`，需录入的用表单弹窗；④ 日期统一 `formatDate`（YYYY-MM-DD）与 `formatDateTime`（YYYY-MM-DD HH:mm），上海时区；客户端组件禁止用 getHours()/getDate() 等本地时区方法做展示与日历计算，改用 `src/lib/ui/sh-time.ts`（否则服务器与浏览器时区不同会水合失败）；⑤ 列表筛选统一分段控件或 `FilterSelect`；⑥ 已实现能力须有可达入口，并按配置与权限显示。

**自然人证件类型与校验（2026-09-14 用户确认，含数据库变更）**：客户与当事人共用 `ClientIdType` 证件类型——自然人可选居民身份证（默认）、港澳居民来往内地通行证、台湾居民来往大陆通行证、护照、外国人永久居留身份证、其他；机构为统一社会信用代码。`Party.idType` 可空，历史数据为空时按身份证处理。选择身份证时输入只允许数字与末位 X、自动转大写、限 18 位，位数不对不能通过（前端即时提示 + 服务端 zod 同规则）。有统一社会信用代码的主体输入名称时通过元典企业信息接口匹配，回填信用代码、注册地址与法定代表人（未配置时静默不影响手工录入）。审批中证件号码直接明文展示。

**冲突预检与正式冲突检索分离（2026-09-14 用户确认）**：系统中有两处冲突检索，定位不同。① **正式检索**在收案流程内：收案向导提交时自动携带全部主体检索并挂在该收案上，收案详情给出结论（可承接 / 有冲突 / 信息不足），收案审批核查，转正式案件前把关；未命中时系统记为「未命中（系统自动提示）」。② **工作区「冲突预检」**（顶栏「冲突预检」按钮、首页快捷弹窗、客户档案入口；2026-09-18 起不在侧栏占一级导航）是律师收案前自行了解本所记录：不经审批、**不出任何结论**（`ConflictCheck.intakeId` 为空、`conclusion` 恒为 PENDING，`setConflictConclusion` 拒绝预检记录）、不能挂靠或替代收案检索；页面按检索主体全面展示命中的历史案件与在办收案、命中方式、对方角色与风险提示，未发现不表述为无冲突。预检仍留存检索记录并写审计，但页面不再展示「既往预检记录」（2026-09-18 用户确认：只有主体名与命中条数、点不开、且很快过期，回看无价值，留痕由 ConflictCheck 与审计日志承担）。两处检索范围一致：历史案件当事人（含单位信用代码列）、客户档案关联案件（证件用盲索引比对）、以及尚未转为案件的在办收案（INTAKE / PENDING_CONFIRMATION / NEEDS_REVISION，正式检索排除收案自身）；在办收案命中只披露收案名称、登记人、状态、登记日期与命中角色，不返回收案 ID、不开放收案内容。

**侧栏信息架构（2026-09-19 用户确认）**：单列不分组——工作台 / 案件 / 审批 / 日程 / 客户 / 财务 / 报表，条目不多，分组标签只会增加噪音（先后作废过「工作区 / 业务 / 资料」与「办案 / 经营」两版分组）。「更多应用」是工具抽屉不是导航项，固定在底部用户卡上方。**法院短信不进导航**：它是有时效的外部来件队列（送达回执、开庭传票），入口在顶栏与通知铃并排，图标带未处理角标（开庭 / 举证 / 缴费类标红），点开列未处理短信并可就地「标记已处理」或「去处理」跳 `/inbox?focus=<id>` 定位高亮；整页 `/inbox` 保留分诊能力。收案不占侧栏一级——`/intakes` 列表早已并入 `/matters?tab=intake`，「收案中」「收案待补正」是案件列表里与办理中同级的页签，新建入口在顶栏主按钮。冲突预检不占侧栏一级——入口在顶栏「新建收案」左侧的次级按钮（先预检再建案的动线）；归档不单列分类——「已归档」本就是案件列表与办理中、收案中同级的页签，归档台账（归档号、归档日期、结案原因、卷宗目录）作为该页签下的「归档台账」链接进入 `/archive`。团队只读视图与案件详情共用同一套墨案版式（案卷头 + 程序栏 + 主栏/侧栏），只换外观，可见内容与权限不变。

**案件详情页「案卷工作台」（2026-09-14 用户确认第三版）**：案卷头 → 程序栏（浅色程序卡片，点击切换，全页共用）→ 页签「案件档案（默认首屏：基本信息含当前程序字段、当事人紧凑卡片）/ 办案进程 / 委托与财务 / 审批用印」，侧栏（承办团队、最近待办、最近记录）在各页签常驻。办案进程按环节组织：左列圆形节点竖排环节（末尾「＋ 添加环节」，窄屏变横向）+ 竖列首项「总览」（与环节同样式）+ 环节操作条+ 待办 + 材料（证据要点作为标签挂在材料行上，不单设证据链模块）+ 经办记录（已发生事项，含快递与备忘，不含材料）。**同一数据只在一处展示**。细则见 `docs/UI-MATTER-DOSSIER-PLAN.md`。视觉：深墨头版 + 压边程序卡 + 吸顶胶囊页签 + 柔和阴影区块；案件详情页不使用宋体（用户反馈显得廉价），标题统一无衬线粗体。

**开票与收款动线（2026-09-18 用户确认，含数据库变更 v44_fee_confirm_state）**：① 开票不以收款为前提——律师提交开票申请（关联案件必传开票依据，专票校验购方六要素），审批人可在批准时直接上传电子发票与发票号一步到 ISSUED，也可先批准（APPROVED＝待开票）再由具「执行开票」权限的人上传完成；电子发票加密入库并挂案件材料。② 实收一律两步，无人例外（2026-09-19 用户确认）：**任何人登记的实收都先记 `FeeEntry.confirmState = PENDING`**——经办律师、主任律师自己收的案件、财务本人登记的都一样——不生成实收 Payment、不派生分成、不写时间线、不计入已实收与回款进度、不进报表与工作台统计，只在财务页「待确认实收」与案件财务区以琥珀色呈现。确认（`confirmFeeEntry`）与退回（`rejectFeeEntry`，删除该条 + 写审计 + 通知登记人）**仅限持有「确认实收到账」（`finance.confirm`，仅 ALL 范围）的财务管理人员**：内置角色中只有「财务」自带，主任律师不再自动具备；其他人选由管理后台「岗位角色」配置自定义角色授予，人选与权限范围都在后台设置。确认人记在 `confirmedById/confirmedAt`，确认与退回均写审计。③ 发票与实收勾稽：登记收付时可选本案发票自动带出发票号与金额，财务页「开票管理」下的「已开票未收款」列出已开具但无对应实收的发票。

**案件一案一签，不设委托模块（2026-09-16 用户确认）**：收案登记的收费约定、委托合同与合同扫描件统一并入「案件档案 · 委托与收费」一个字段表（2026-09-18 用户确认：合同仅指委托代理相关合同，不另立「合同与材料」子框）；中途变更收费或增加代理程序＝在「委托与财务」新增一条补充协议（Billing），原合同保留、合同额合计，不改原合同也不新增模块。

**案件详情页的事项与记录（2026-09-15 用户确认）**：办案进程只有两个添加入口——「＋事项」（任务 / 期限 / 开庭，进待办）与「＋记录」（沟通 / 寄收件 / 研判，进经办记录）。「备忘」不再新增，等同于无截止日的任务；历史备忘作为事项显示在待办中，可办结、删除。快递不手写状态，填单号后由接口更新。完成的事项与系统事件自动进入记录。

**日历订阅入口**：统一放在日程页右上角，以「订阅日历」次级按钮打开弹窗，管理订阅说明、复制链接和重置链接；个人设置不再展示订阅卡片。弹窗组件就近放在 `schedule/_components/`，仅打开时获取订阅链接，沿用现有订阅权限与接口。

---

### 五、开发纪律

1. **改文档优先于改代码**。需求或规范变了，先改 `docs/` 和本文件，再写实现。
2. **新模块顺序**：数据模型 → 类型 → Server Action → UI。不允许先写 UI 再补数据。
3. **业务规则集中**。状态机（`Intake → Matter` 转化、`Matter` 阶段跃迁）、金额计算、权限判定，统一在 `src/lib/` 或 `src/server/`，不要在 React 组件里写。
4. **不为了让代码跑起来**注释掉校验、绕过权限判断、`@ts-ignore` 整段。找根因。
5. **密钥、token、密码**不进代码、不进 commit、不进日志、不进截图。`.env` 永远在 `.gitignore`。
6. **Prisma 迁移**先用 `prisma migrate dev --create-only` 生成 SQL 给叶森看，再执行。生产迁移走 `prisma migrate deploy`。
7. **删除文件、`.env` / CI 修改、`git reset/rebase/push`、生产部署**必须先征得叶森确认（红线）。
8. **附件**默认私有存储在 `storage/`，下载经鉴权 API，不暴露公开直链。

---

### 六、权限与审计

#### 业务端与管理后台分离（2026-09-06 确认）

- LawLink 保持单体应用、单库和统一登录；新增 `/admin` 独立管理后台页面壳，不另建部署或日常管理员账号。本人账号可同时承担业务岗位和系统管理职责，审计始终落在同一 `User.id`。
- 业务端“个人设置”只保留本人资料与登录安全；律所信息、用户、岗位角色、律师团队、审批权限、归档制度、模板、自定义字段、外部接入和审计日志统一从管理后台进入。管理后台持续显示当前处于系统管理模式，并提供返回业务系统入口。
- 第一阶段曾只分离路由、导航与页面壳；第二阶段已按 `docs/ADMIN-CONSOLE-PLAN.md` 以全新版本标准拆分业务岗位、系统管理资格和审批资格，不保留旧数据、旧角色或旧网址兼容逻辑。
- `/admin/*` 为后台唯一入口，`/settings` 只保留个人资料与登录安全。系统管理操作统一校验 `SUPER_ADMIN`；主任律师原有提醒维护和批量导入资格保留。系统身份变更须失效旧会话、记录审计并保护最后一名有效超级管理员。`UserRole` 不包含混合型 `ADMIN`，超级管理员另持真实业务岗位。Schema 变更和旧路由删除已批准；新迁移 SQL 在执行前仍须单独获得明确批准。
- 新管理页面入口位于 `src/app/(admin)/admin/`，管理页面壳位于 `src/components/layout/admin-shell.tsx`；权限判断仍在服务端既有位置执行，不能只靠隐藏导航限制访问。

#### 自定义岗位角色（2026-09-06 批准）

- 按 `docs/ROLE-MANAGEMENT-PLAN.md` 实施角色管理及用户分配。保留五个内置角色，新增 `CUSTOM` 与角色定义、权限配置；每人一个岗位角色，审批权限组仍独立配置。
- 仅超级管理员管理角色；“行政”按既有固定角色记录标识纳入内置角色。全部内置角色允许编辑显示名称和介绍，不能修改权限、停用或删除；名称不参与授权判断。五个原有角色的显示配置复用 SystemSetting，“行政”沿用现有 RoleDefinition，不改 Schema、不迁移账号。自定义角色支持编辑和停用但不物理删除。停用自定义角色暂停所属账号业务访问及当前审批资格。
- 内置角色名称与自定义角色名称统一校验重名；显示资料修改检查并发版本、事务内复核管理员并记录审计。改名不撤销会话或改变审批、财务、团队等授权；界面和人员列表使用当前显示名称。
- 自定义角色按固定功能权限和数据范围授权，未知配置默认拒绝；读、写、财务、附件及导出分别校验。团队只读不能授权写入、下载或导出，申请级审批不能授权案件全文。
- `src/lib/roles/` 存权限目录、解析和范围规则，`src/server/roles/` 存角色校验及事务管理，`src/app/(admin)/admin/roles/_components/` 存角色页面组件；文件用英文短横线，测试在 `src/tests/` 对应目录。
- 本次方案和 Schema 修改、生成迁移 SQL 已获批准；执行数据库迁移仍需展示 SQL 后另行确认。create-only 不支持时允许仅对本次前后 Schema 使用 migrate diff 生成 SQL，禁止重置数据库或混入其他迁移。

#### P0 底线补强批次决策（2026-09-13 批准，叶森授权按方案直接执行）

- 依据 `docs/P0-IMPLEMENTATION-PLAN.md` 与 `docs/TARGET-MODEL-PLAN.md` 实施改进报告 v5 的 P0 八项。本批 Schema 变更与迁移 SQL（20260913052823_p0_baseline_batch）已获批准并执行；后续各批迁移继续走 create-only/diff 生成、展示 SQL 后执行的流程。
- **客户主数据（P0-1）**：身份持续唯一策略——证件类型 + 规范化号码部分唯一索引（仅约束已填号码且未删除的行），存量行补录证件类型后纳入约束。建档、收案、批量导入三入口统一查重；匹配复用与存量合并分别验收；合并只调整当前主体关联，不改写历史身份快照，保留映射与合并依据。
- **登录锁定（P0-3）**：连续 5 次失败锁 15 分钟，成功登录清零，锁定拒绝写审计。
- **期限来源（P0-8）**：规则生成的期限以待确认状态落库；已确认/已调整期限不被规则重算静默覆盖；法定期限规则管理界面位于管理后台"提醒维护"页，内置规则不可删除。
- **制度决策 4.1（团队历史访问，选项 A）**：维持团队汇总默认，新增案件级"受限事项"标记（`Matter.teamAccessRestricted`）——受限事项不进入团队汇总视图，限制优先于团队授权；个人直接授权不受影响。
- **制度决策 4.2（证件采集）**：维持现行默认（实名账号登记证件并留存照片）；账号实名、身份核验、证件照片留存按目的分别评估的原则记入本文档，未来如引入受限协作账号按该原则另行设计。
- **制度决策 4.3（低影响自确认，2026-09-13 已实施）**：管理后台「审批权限」页提供自确认清单（文书送审/用章申请[非法人章]/收案审批三项可配），命中清单的提交由申请人自我确认即完成原审批环节，审计动作标记 `*_SELF_CONFIRM`。归档、开票、盖章回填与法定代表人章（含 requiresLegalRep 章种）由服务端硬排除，任何配置不可自确认；附带法人章的用章申请整单不走自确认。收案审批清单可配，转化接线随收案专项启用。实现位于 `src/lib/approvals/self-confirm.ts` 与 `src/server/approval-permissions/self-confirm-actions.ts`。

#### 个人身份与基本资料（2026-09-06 确认）

- 本人可维护姓名、手机号和登录邮箱；邮箱变更校验当前密码、撤销旧会话。角色和权限不在个人资料中修改。
- 人员最终身份标识采用“证件类型 + 规范化证件号码”，内部业务关联始终使用 `User.id`。支持居民身份证、港澳台居民居住证、往来港澳通行证、港澳居民来往内地通行证、台湾居民来往大陆通行证、护照、外国人永久居留身份证和自定义其他证件；`OTHER` 必须填写证件名称。停用账号仍占用复合唯一标识。
- 管理员新增用户时必须选择证件类型并登记号码；**证件照片为选填**（2026-09-13 修订：账号开通与证件照片采集解耦——照片可在开通后经「资料 → 身份证件」补充上传，涉及身份核验的场景按需在核验前采集）。视觉识别只作预填，管理员必须对照原件确认。非本地 AI 服务每次识别前展示服务域名并取得当次确认，识别失败允许手工录入，不把 OCR 表述为实名认证或真伪鉴定。
- 证件照片独立于案件 `Document`，始终用 AES-256-GCM 加密写入私有 `storage/user-identities/{userId}`，只能由本人或 `SUPER_ADMIN` 经鉴权接口查看。旧照片更正时标记被替代并保留审计，不物理删除；不得进入案件权限、通用导出、全局搜索或公开直链。
- 证件号码、手机号在有权限的页面内直接明文展示，不做打码与「明文/隐藏」切换（2026-09-18 用户确认：律师办案需随时核对号码，反复展开浪费操作）；访问控制仍靠权限而非遮蔽——证件号码与图片只允许本人或管理员查看。自动识别、号码查看、图片查看、上传替换和管理员更正均审计，日志不得记录完整号码、图片、原始文件名或 AI 原始响应。
- 历史账号未登记号码时保持可用，不推测或回填。资料修改与审计同事务提交，并校验当前权限、账号状态及并发版本。
- 实现沿用 `src/server/users/`、个人信息页与用户管理页；证件规则集中在 `src/lib/identity-documents/`，识别、上传和访问控制集中在 `src/server/identity-documents/`，跨页表单放在 `src/components/users/`。设计说明见 `docs/PERSONAL-PROFILE-PLAN.md` 与 `docs/IDENTITY-DOCUMENT-PLAN.md`。本次 Schema 修改已批准；迁移执行必须在正式 SQL 展示后获得确认，已执行迁移不得重复运行。若 `migrate dev --create-only` 因非交互环境无法运行，允许用 `prisma migrate diff` 对本次修改前后 Schema 生成 SQL；该替代仅生成文件，不得重置数据库或执行迁移。

#### 按事项审批（2026-09-06 确认）

- 岗位角色与审批授权分开；管理员管理账号、审批权限组与用章事项。审批规则以操作、案件类别、用章事项、印章类型组合，组间取并集、规则内取交集；空选择不代表全部。
- 审批授权仅覆盖对应申请及必要附件，不扩大案件成员、团队、财务、导出权限；团队负责人不自动获得审批权。
- 收案、文书、归档、开票、用章审批及盖章回填分别授权。所有人员（含超级管理员）审批法定代表人章时核对当前法定代表人；默认不得审批本人申请，单人执业例外由管理员显式开启并审计。
- 授权、停用与账号状态实时检查。权限变更和审批结果与审计同事务提交，已处理申请不得重复处理。
- 所有账号（包括 `SUPER_ADMIN`）统一按事项授权，系统管理身份不提供审批或盖章回填捷径。无合格审批人阻止提交；新系统不读取旧角色审批授权，也不提供历史未分类申请的补分类入口。审批人仍须遵守账号有效性、事项及印章启用状态、事项与印章适配、本人审批开关、法定代表人章身份限制、申请状态和审计要求。
- 实现目录：`src/lib/approvals/` 放集中规则与鉴权，`src/server/approval-permissions/` 放管理操作，`src/app/(admin)/admin/approval-permissions/` 放管理界面，`src/app/(app)/approvals/` 放统一审批工作台；测试沿用 `src/tests/`。设计与迁移说明存 `docs/`，迁移 SQL 存 `prisma/migrations/`。生成 SQL 不等于授权执行迁移。Playwright 验收输出放 `output/playwright/`；CLI 首次运行产生的 `.playwright-cli/` 仅为本地临时快照，不入交付，清理前遵守删除确认规则。

#### 统一审批工作台（2026-09-06 确认）

- 后续使用以全新系统的完整流程为准：收案冲突检索自动携带申请中各主体的身份，关联收案的检索不得省略身份；审批主界面直接展示本案核查对象及身份、具体命中和人工意见，不以旧数据兼容解释组织页面。查询条件及既往检索仍可展开复核；资料不完整时直接提示重新检索，不推测身份、不清空记录。

- 立案审批视觉方案已确认：宽幅审阅窗口、顶部案件摘要、申请资料/冲突核查/附件/处理记录页签、桌面右侧固定审批区及窄屏底部操作。完整字段仍可查看，未填写的非关键项可折叠，风险在首屏提示；切换页签保留意见，沿用全部授权及校验。呈现组件及局部 CSS 放在 `src/app/(app)/approvals/`，不改变其他审批类型；细则见 `docs/UI-APPROVAL-DETAIL-PLAN.md`。
- 叶森已确认将同类视觉优化扩展到其他审批与弹窗：审批采用统一宽幅审阅框架，普通表单与确认框按内容保持适当宽度。公共标题、表面、操作区及响应式规则在原子层统一，审阅布局与分区组件放 `src/components/patterns/`；分批迁移实际审批和常用表单，保留入口、字段、校验、授权和审计。实施清单见 `docs/UI-DIALOG-CONSISTENCY-PLAN.md`。

- 立案审批详情按原填写项目完整展示已保存的申请信息（含程序、当事人、标的、服务范围、收费、承办人员及附件），敏感信息按申请查阅权限直接明文展示，不做打码与展开切换。冲突审查展示各次检索对象、命中字段及理由、风险提示、人工结论及说明；未命中不表述为人工确认无冲突。关联案件仅展示冲突核查所需的编号、名称、主办及命中角色，不因审批授权开放其他案件正文。关联档案当前值与历史检索记录须明确区分；缺失快照不得补造。
- `/approvals` 统一收案、文书、归档、开票、用章的待办、历史、详情及执行环节；提供待我审批、我已处理、我的申请、全部记录（仅 `SUPER_ADMIN` 审计查看）四个视图。已批准待开票/待盖章单独标明执行环节，不能再次驳回已批准事项。
- 申请人、实际处理人可查看该申请及处理历史；收案主办保留原有补正和重新提交权限；当前审批授权仅提供当前待处理环节的访问；超级管理员可审计全部申请，并按上述超级管理员规则处理当前有效环节。以上不扩大整个案件的访问、写入及导出权限。
- 历史优先展示逐次审计事件，业务字段仅补充有明确人员和时间的旧记录；缺失信息标明未记录，不推断。详情明确区分当前申请内容和历史处理记录。新处理意见写入同事务审计，重复送审不得抹掉前次意见。
- 申请附件逐件校验；实际处理人可读取该申请关联的原附件，重新提交后新加的附件不按前次处理关系开放；历史查阅不开放案件的其他附件。统一工作台文件沿用 `src/lib/approvals/`、`src/server/approval-permissions/`、`src/app/(app)/approvals/`，不新增数据库表或迁移。

#### 归档材料审阅与完整性（2026-09-06 确认）

- 归档清单必须关联实际案件材料。申请人勾选、文件存在和审批人确认是三个不同事实；无文件的必交项不得显示为齐全。必交缺项或不适用须逐项说明，审批人明确核准例外后才能通过。
- 管理员在“设置—归档制度”确认本所采用的制度名称、版本、生效日期及制度原文。申请固定送审时的制度与清单版本；未配置本所制度时不得提交正式归档申请，不把系统内置参考清单表述为本所制度。
- 归档申请固定当次文件名称、大小、内容校验值、清单关联和顺序。审批详情逐项展示实际材料、缺项说明、人工核验事项及制度依据；归档审批授权只开放该申请固定的材料，不扩大案件正文、财务、其他附件或整案导出权限。
- 提交和审批通过时均由服务端核对文件归属、有效性和内容校验值；待审文件不得删除。旧申请不能还原材料范围时标记为历史未固定并退回补正，不按当前案件材料推断过去齐全。
- 驳回后重新提交形成新申请并保留旧记录。批准后的归档包严格按该条批准记录固定的材料生成；文件缺失、读取失败或内容变化时停止导出，导出路径和校验值只写回目标归档记录。
- 实现复用 `ArchiveRecord.checklistJson` 保存有版本的材料快照、`SystemSetting` 保存归档制度配置、`AuditLog` 保存逐次处理与审批核验。本期不改数据库 Schema；历史布尔清单只兼容读取，不升级为已核验快照。

#### 律师团队（2026-09-06 确认）

- 常设律师团队与案件承办人员分开。仅系统超级管理员（`SUPER_ADMIN`）创建/停用团队、指定负责人、设置成员和额外查看人员；团队负责人不因此获得全所管理角色。
- 负责人及获授 `canViewAllMatters` 的成员，自动查看当前团队成员原始立案或当前主办的全部案件（含历史案件）；不要求逐案关联团队。协办关系不向协办人员的整个团队传播权限。
- 按当前有效团队、有效成员关系实时判定；退出团队、撤权或停用团队即停止新增的团队访问，个人已有经办权限保留。账号禁用不使其历史案件从团队汇总消失，但禁用账号不得继续访问。
- **保留团队历史访问自动汇总**（2026-09-13 复议维持）：一至五人小所人员调组频率低、历史回看高频，自动汇总的实际价值大于扩散风险；越权面由案件级受限标记（`teamAccessRestricted`）收窄。**撤权复核提示**：管理后台保存移出成员、更换负责人或停用团队时，界面须提示"该成员经团队汇总可见的历史案件访问即时停止，如有仍需其访问的受限案件请另行复核"；提示不阻塞保存。
- 原始立案人：收案转化取原收案创建人，直接立案取创建人；历史资料只按收案记录/创建审计回填，无法确定时留空。批量导入保留操作人记录，当前主办仍纳入汇总。
- 团队新增授权是只读案件正文、进度、期限及记录的授权；财务、分成、附件下载、导出和业务写入沿用个人经办/既有全所角色权限。禁止使用只读访问断言授权写入。
- 实现约定：`src/server/teams/` 放团队校验和服务操作，`src/lib/teams/` 放共享选择规则，`src/app/(admin)/admin/teams/_components/` 放团队设置页组件。沿用英文短横线文件名；测试在 `src/tests/` 对应目录。迁移及审阅说明分别存 `prisma/migrations/`、`docs/`；不另建临时产物目录。

#### 制度级决策（2026-09-13 确认，改进报告 v5 第八章）

- 决策程序：v5 报告第八章将「团队历史访问默认」「建号必传证件照片」「普遍按事项审批」三项列为例行复审项（第三项已于 2026-09-06 落地为按事项自确认配置，不再重复决策）。叶森于 2026-09-13 将余下两项的决定权委托给实施方，本节即修订记录，先改本文件、再改代码。
- **团队历史访问默认：保留自动汇总**。比较要点：本产品定位一至五人小所，调组频率低而历史回看高频；越权阅读后果已由案件级受限标记收窄，撤权即时生效已有实现。配套补齐撤权复核提示（见律师团队一节）。若未来部署到十人以上团队，应重新评估改为显式授权。
- **建号必传证件照片：解耦**。账号开通仍强制证件类型与规范化号码（最终身份标识），照片改为选填、可在开通后补充；是否采集照片按身份核验目的单独判断，不由账号开通强制。理由：账号实名、身份核验与照片留存是三件事，强制耦合使新成员入职即被挡，而照片的留存价值依赖核验场景。

- **业务岗位**（V1）：`PRINCIPAL_LAWYER` / `LAWYER` / `ASSISTANT` / `FINANCE` / `CUSTOM`；系统管理身份另用 `SystemRole.SUPER_ADMIN`，不得从业务岗位推导。
- **可见性**：`Matter` 保留原有个人/全所角色权限，并按上文律师团队规则增加团队只读权限；`FINANCE` 可见所有案件的财务字段但不能编辑案件正文。
- **跨案件最小披露例外——冲突检索**：冲突检索结果会跨越案件可见性，向检索人展示命中案件的**系统编号、案件名称、主办律师姓名、当事人角色**，但不给出跳转链接、不返回 `matterId`。理由是不跨案件就无法尽职审查，且检索人需要知道找谁核实。**例外仅限于此**：案件正文、财务、材料一律不得因冲突检索而对非成员开放。
- **审计日志**（`AuditLog`）必须记录：登录/登出、案件创建/查看/编辑/归档、材料下载、财务变更、冲突检索、权限变更。
- 第一版**不做字段级权限矩阵**，能用"角色 + 案件成员"覆盖的就不上更复杂的方案。

---

### 七、验证命令（每次改完主动跑）

```bash
npm run lint              # ESLint CLI
npm run typecheck         # tsc --noEmit
npm run prisma:validate   # Prisma schema 校验
npm run build             # 生产构建（最严的检查）
```

UI 改动还要：在浏览器里把"金线"（典型工作流）走一遍，不只看类型/构建过。

每次修改后还必须确认本地站点仍可访问：先核对端口监听进程属于 `/Users/yesen/Code/LawLink`，再打开 `http://localhost:3000/login` 或本次涉及页面。若服务已停止或端口不属于本仓库，先恢复/切换到正确 LawLink 服务并重新验证后再交付；不能只报告代码验证通过。

---

### 八、安全底线

- 附件私有，下载链接带短期 token。
- 案件正文、客户联系方式、身份证件号码、身份证件照片、统一社会信用代码视为敏感数据：控制在「谁能进这个页面」，进入后直接明文展示，前端不再提供“打码/明文”切换（2026-09-18 用户确认）。证件图片仍按需加载并鉴权。
- 日志中不输出 PII（身份证、电话、住址完整值）。
- `AuditLog` 不可由业务代码删除，只能归档。

---

### 九、文档清单

| 文件 | 作用 |
|---|---|
| `AGENTS.md`（本文件） | 工作区铁律，所有协作者必读 |
| `docs/PRD.md` | 产品需求与功能范围 |
| `docs/DATA-MODEL.md` | 数据模型详细设计与 ER 图 |
| `docs/UI-DESIGN.md` | 设计语言、色板、关键页面 wireframe |
| `README.md` | 仓库门面，安装运行说明 |
| `CHANGELOG.md` | 版本变更（V1 发布时建立） |

---

### 十、给 AI 助手的特别说明

- 默认中文回应，代码/字段/路由用英文。
- 结论先行，方案有问题直接指出。
- 涉及红线（删文件、改 `.env`/CI、`git push`/`reset`/`rebase`、生产部署、Schema 迁移）**必须**先征得叶森同意。
- 不为了"看起来很忙"提交大量改动。每次改动控制在可审阅的范围。
- 自动验证清单见第七节。


### v1.3.0 版本归集（2026-09-13）
- 按用户授权归集北京时间 2026-09-10 之前的未提交开发，以 2026-09-07 历史文件快照为基准。
- 本版本包含后台分离、岗位/团队/审批权限、统一审批、归档审阅及身份资料改造；不包含 9 月 13 日的 P0/P1 和 v4 界面开发。
- 仅发布代码与版本标签，不执行数据库迁移或生产部署；运行数据、合同工作产物及缓存不纳入版本。
- 发布验证发现既有 instrumentation 使用 webpackIgnore 导致生产启动缺失调度模块；本版附带最小打包修复，保留运行时、开发环境和 DISABLE_CRON 判断。


### v1.3.1 公开说明与 CI 维护（2026-09-13 批准）
- 用户已批准修复 GitHub CI，并要求审校公开说明。使用独立发布副本，不纳入下一版在途功能。
- 模型漂移检查必须使用独立 shadow 数据库，全新安装测试保留自己的空库；不得通过跳过校验、基线标记或清空业务数据规避失败。
- README、安装/升级说明和安全说明按已发布功能核对；旧设计与体检报告标注历史范围，不把未来计划或旧安全审计结果作为当前保证。
- 维护版 v1.3.1 保留 v1.3.0 标签；本次不操作本地业务数据库或生产部署。


### v1.3.2 依赖安全维护与空白安装（2026-09-13 批准）
- 用户要求修复依赖问题，并明确现有案件数据为模拟测试用途；公开系统不携带案例业务数据。
- 在独立发布副本升级现有依赖的兼容修复版本，验证后发布维护版；保留下一版在途功能。
- 默认初始化只包含管理员和系统基础字典、模板、规则；不得创建案件、客户或财务演示记录。Document 表仅允许系统内置空白模板文件，业务材料必须为空；测试断言验证空白安装；本次不清空本地数据库、不删除用户测试数据或重写 Git 历史。
- 依赖审计按生产与全量分别记录，零已知告警只代表当日审计结果，不等同整体安全保证。现有 CI 增加全量审计与空白业务表断言，沿用已获批准的 CI 维护范围。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
